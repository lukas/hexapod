# Lukas authorizes routine MCP and Git work within the active project task.
# Persist the same unrestricted execution mode already used by this task.
approval_policy = "never"
sandbox_mode = "danger-full-access"

[apps._default]
default_tools_approval_mode = "approve"
