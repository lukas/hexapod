# Six untouched control-chain replays

Prepared for root implementation review; no simulator response has run under this protocol. Root and `fresh_cartesian_frontier` own this finite measurement. The separate preregistration freezes this document and all reference hashes before execution. Execution requires root's independent implementation review, not another user permission question.

## Question and scope

The completed magnitude-allocation STOP lacks synchronized actuator histories. Measure the actual policy → affine decoder → safety → delayed servo profile → effective actuator target → actual joint chain on the same untouched policy. This is observational identification of transfer facts, not another efficacy trial or a controller change. No new action vector, pulse, dose, cadence, contact controller, PPO, seed, physics change or robot work. Every previous STOP and original 5 mrad/retention/continuous-joystick criterion remains unchanged.

## Exact frozen replay matrix and reference

Use checkpoint `ppo_goal_cw_robotwalk_turns_20260907_yawref_cigate8m.zip` SHA256 `61f9c20f0f72d89217a22b47037e33c8dad2f50bfaca0a3d5517f714dd0eeb10`, full XML `7efb8e8a0cb014c0b4bac27c41e7a85e683553168b87d85aac0d52d6a8e5a837`, original 64-key cfg and all source/assets from the magnitude assay. Verify every hash before helper import. Also pin `safety.py` and `hexapod_core/joint_frame.py`, which the old manifest omitted. No implicit current-source overrides.

Exactly six rollouts, **6020 control ticks**: vx .08; yaw ±.15 × initial phases 0/pi for 755 ticks each; yaw 0 × phases 0/pi for 1500 ticks each. Seed 0, DR 0, deterministic policy, original one-second zero hold plus one-second ramp. Arc episode configuration remains 10 seconds; straight 15 seconds. Same frozen helper and full mesh 34 meshes / 159 geoms / 4.80573 kg, 400 write speed / 20 acceleration / .375° control-tick slew / 350 counts/s resolved velocity / 100 Hz. The 350 value is velocity, not current. Original safety/current configuration remains unchanged.

References are the four continuous baselines and two untouched straight baselines in `turn_magnitude_execution_20260908/full_recovered/`, published in `be4e8a423`. Compare all saved 755/1500-tick NPZ arrays, including current, exactly. Recompute the same body trace hash including modes. Compare arc full-state boundaries 600/638/680/718 and both selected prefixes/endpoints (619/657/699/737) to their existing records. The old arc baselines did not persist the complete state at 755: record it as a **new endpoint**, never claim an old 755-state comparison. Straight full state at 1500 must match exactly. No new reference generation or selective replacement.

## Observation timing and noninterference

All hooks call the original operation exactly once with identical arguments and return its original object. Copies go into a recorder outside environment, safety and profile state. Temporary class-method hooks dispatch only for the one live safety/profile object, then restore in `finally`, preserving the classes and instance dictionaries used by full-state fingerprints. Environment method wrappers preserve the same registered state. The reviewed helper's dynamics-audit subclass records existing arrays immediately before/after the original `mj_step`, without an additional solver step, `mj_forward`, kinematics refresh, profile tick, command, `_read_state`, goal-clock query or RNG call.

For every policy/control tick, capture the actual policy observation, returned action, recurrent-state digests when present, decoder input and returned proposed target, previous safety target and actual returned safety target/status, requested slew settings, final latched logical command, actual pre/post joint state and existing post-step sensed joint state/current. The action is dimensionless. Decoder/safety/latched/sensed joint positions are **robot-absolute radians**.

Record each real `profile.command` call independently, with control index, simulator time, profile clock, actual command argument, requested speed (degrees/second), acceleration register units and actual enqueued write/caps. An absent call means no write; never infer a repeated command. Record each real profile tick's before/after `goal`, `target`, `_v`, `_vel_now`, `_acc_now`, queue length and both clocks, plus the corresponding actual physics step's pre/post joint positions/velocities, actuator force and effective position-actuator `ctrl`. Profile/physics coordinates are **native MuJoCo-relative radians**, velocities rad/s and accelerations rad/s². Knee-relative equals knee-absolute minus hip-absolute; no diagonal conversion assumption. Record actual affine centers/half-ranges and profile latency/deadband/default caps, model actuator parameters and resolved motor configuration. Configured caps are not realized velocity history.

Control index starts at zero after the unchanged reset; `data.time`, profile `_t`, episode `_step_i` and substep index remain separate fields. Physics coverage derives from the actual fixed timestep/substep count. No dropped samples or zero-filled missing fields are accepted. Profile deadband gating and the downstream actuator dead-zone are separate observations. Delayed writes mature per joint; queue length alone is not a latency estimate.

The original material-contact recording window is preserved: arc helper start600, straight start201 for post-ramp material observations, as in the reference magnitude replay. All new control-chain arrays cover the full episode, including the initial hold/ramp. Additional source-state capture is read-only.

## Fixed acceptance and descriptive read

The sole measurement decision is `VALID_OBSERVATION` if all six prescribed cells complete exactly, every required sample is finite and indexed once, all per-tick decode/safety/profile/physics operation counts agree with the actual fixed substeps, and every existing trace/current/full-state comparison passes. Missing records, a termination, mismatch, extra/missing cell or stage-call inconsistency means `INVALID_OBSERVATION`; retain all partial data, report the exact failure and stop. No efficacy score or permissive tolerance replaces byte-exact reference parity. Only source arithmetic/frame cross-checks may use explicitly declared float64 tolerances; they never apply to recorded reference arrays.

Predeclare descriptive aggregates on all ticks and on fixed post-ramp ticks [200,N): per-joint mean absolute decoded-to-safe difference; requested versus delivered safety increment magnitude; per-joint safe slew saturation fraction; profile goal-target tracking error and velocity/cap utilization; effective actuator-target minus profile-target displacement; actual target-to-joint errors; commanded/profile/actual direction reversals with zeros ignored; sensed-versus-native-frame joint differences. Preserve raw arrays and per-cell/per-joint results; do not choose a favorable phase/window or fit delays/gains/templates from these outcomes. Any regression or ratio is descriptive, not an intervention effect.

A valid result can distinguish measured stage attenuation, delay and reversal patterns on this trajectory. It cannot prove that a corrective law would improve yaw, uniquely attribute contact/body-motion loss, establish a controller-independent limit, or identify post-pulse policy cancellation without a paired intervention. It does not qualify walking with joystick yaw or authorize a future controller/PPO. A subsequent law would need its own distinct mechanism, preregistration, comparator, original gates and ownership review.

## Finite failure handling

No automatic rerun or selective cell retry. Root may review one instrumentation recovery only for a demonstrated pre-response implementation error, preserving its original failure and unchanged protocol/reference hashes before any execution. A failure after responses start remains evidence and ends this finite execution pending root review. Expected cost is six CPU rollouts, well under a GPU workload; no fleet launch is part of this task.
