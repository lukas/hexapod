# Pre-response implementation package

No simulation or frozen rollout helper was imported/executed during preparation. Root reviews this package before the six baseline replays run. Current status: **prepared and preregistered; awaiting root implementation review**.

`capture_control_chain.py` imports only Python standard-library modules and NumPy at module load. Its execution entry point verifies the protocol, 13 exact reference/helper artifacts, all original pinned source/assets, the checkpoint, and additional safety/joint-frame hashes before loading the frozen helper. The runtime remains the previously validated isolated full-mesh source, not current main's different sim_env/servo_model files.

The recorder observes existing operations once, with original argument/return objects. Class hooks leave the fingerprinted safety/profile instance dictionaries unchanged; environmental hooks restore their original attribute ownership, including on failure. A subclass of the existing dynamics audit surrounds its original physics step. Every read copies existing arrays; no new solver/kinematics/state refresh, profile tick, command, goal query or random draw is introduced. Initial pending writes and every subsequent command, profile update and physical substep are recorded separately. The runner is sequential and stops on failure; it cannot select a replacement cell or create an extra rollout.

`test_capture_control_chain.py`: **60 passed in 0.10 seconds**, run locally via `uv run --active --offline pytest -q -p no:cacheprovider`, with bytecode writes disabled. These tests use mock control/physics objects and the hash-verified pure joint-frame module. They cover six exact cells / 6020 ticks, actual knee-frame conversion, missing/nonfinite/ragged records, exact shape/dtype/bytes reference parity (including signed zero), operation counts/order, no-write semantics, original argument/return identity, hook dispatch/restoration under failure, and time/coordinate continuity. Clock arithmetic tolerances are 1e-10 seconds (dt declaration 1e-12); affine reconstruction tolerance is 2e-15 radians. These are not reference-parity or efficacy tolerances.

The tests exposed and repaired two issues before any response: NumPy value equality was too weak for the promised byte-exact comparison; and a shuffled command table could satisfy count equality. The final code compares shape/dtype/bytes and requires chronological command events. Added adjacent substep continuity checks prevent a middle-of-tick gap from passing endpoint-only checks. The test module also resolves the pinned joint-frame source from the selected runtime rather than an agent's temporary directory.

Static checks establish instrumentation contracts, not dynamic noninterference. The six prescribed full-trajectory/current comparisons and all previously saved complete-state boundaries are the required dynamic verification. The four old arc traces have no saved complete755 endpoint; those endpoints are new captures and are never substituted into the old reference. Straight1500 complete endpoints have existing references and must match exactly.

No new controller, pulse, efficacy result, PPO run or robot action is part of this package. Prior STOPs remain closed. A valid measurement only supplies descriptive transfer facts; a proposed corrective law would require a later independently reviewed design and the original qualification gates.
