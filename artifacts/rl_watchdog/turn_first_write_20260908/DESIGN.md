# Archived first-write transmission diagnostic

**Prepared for root review; no diagnostic output has been computed.** Root and `fresh_cartesian_frontier` own this finite offline task. Preregistration freezes the implementation, inputs and state inventory before execution. This protocol contains zero simulator rollouts, policy evaluations, new controller actions, PPO or physical robot work.

## Why this question survives

The unchanged control-chain measurement in commit `f31a9f688dd466f0224150832ae235be7fe508ec` was valid and showed frequent proposed increments beyond the safety slew allowance. It did not measure transmission of any intervention. Prior magnitude `clip_hits=0` meant no clipping to the action box; it did not test the stateful slew limiter. Thus the exact initial command change produced by the failed archived pulses is still missing.

No corrective law is justified merely by these aggregate gaps. In particular, a limiter-aware reallocation or change of injection layer could be another ineffective perturbation, and existing nonzero paired body responses already exclude complete five-tick causal masking for those branches. The smallest useful next step is reconstructing their first write, where the unchanged and intervened policy share a verified identical pre-state. After that first step, baseline-only reconstruction cannot substitute for the missing closed-loop branch actions and safety states.

This diagnostic distinguishes **action-bound clipping, exact first-write safety masking, and partial/nonzero first-write safety transmission**. It does not remeasure efficacy, fit response magnitudes, retune a sign/support template, alter dose, change phase timing or test another time-slicing law. Completed single-axis, sign-box, geometric support, event/timing, magnitude and time-slicing STOPs remain unchanged.

## Exact inputs and scope

Use all 32 archived signed magnitude branches from result commit `be4e8a42381f128ead7bc86486f295de68940d89`: eight existing frozen states, each with +candidate, −candidate, +comparator and −comparator. Read their original first-dose records and exact previously frozen candidate/comparator vectors. All states remain in their original groups and use the original ticks 619 or 657. No selection from the measured yaw responses, subset, new state, seed or dose is allowed. The maximum original requested coordinate magnitude remains .05.

The matching policy actions, previous safe targets, safety statuses/caps and actuator-write map come from the six complete control-chain NPZs. The runtime is not imported. Validate arithmetic against **all 6,020 baseline samples**, including all four 755-tick arcs and both 1,500-tick straight traces. Then reconstruct the 32 onset writes only. Exactly eight zero-onset checks accompany those 32 calculations. Unit-test positive controls exercise known unsaturated transmission, partial saturation, full masking and slew-then-joint-limit order; these synthetic controls generate no policy or plant responses.

The original checkpoint SHA256 remains `61f9c20f0f72d89217a22b47037e33c8dad2f50bfaca0a3d5517f714dd0eeb10`; full XML `7efb8e8a0cb014c0b4bac27c41e7a85e683553168b87d85aac0d52d6a8e5a837`; original cfg `aabf4cc25f78ebf3b28ff7b4a46fba85c109f4061becaf4212e8842b5c550e40`. The data retain full mesh, 34 meshes / 159 geoms / 4.80573 kg and the motor contract 400 write speed / 20 acceleration / .375° control-tick slew / 350 counts/s resolved velocity / 100 Hz. The 350 value is velocity. No source, motor, current, safety, mesh or physics setting is changed by offline arithmetic. Pin the actual affine decoder, safety and joint-frame source files for code-comparison tests; do not import their runtime dependencies or substitute newer simulation behavior.

## Calculation and exactness

For an archived onset at tick P, use **control row P**, the pre-action state after P completed ticks. Require agreement among frozen state identity, archived branch complete-prefix hashes/qpos and the measured baseline complete state. Require the original five dose records at P through P+4, but consume only the first for this diagnostic.

Reproduce the original injection exactly: copy the baseline policy action as float32, add the original signed float64 vector, clip to [-1,1], then cast the changed action to float32. The archived applied increment was the changed action minus the original action in its original dtype. Require exact equality of reconstructed first requested/applied vectors and archived action-clip count. Do not replace the archive with an idealized decimal pulse.

Map the changed action with the recorded affine centers and half-ranges. Apply the pinned safety arithmetic: previous safe target plus the clipped requested delta at the recorded cap, then the fixed physical joint-limit clip. Health and IK status at this first step are unchanged: the shared pre-state is healthy, the fixed affine decoder accepts every finite action, and the safety health branches depend on that shared state. If any recorded health/hold/termination/entry-ramp/write assumption fails, invalidate this audit instead of taking a favorable subset.

Finally map robot-absolute safe targets to native MuJoCo-relative write coordinates. Knee-relative = knee-absolute − hip-absolute. Validate baseline policy-to-decoder identity, decoder output, safety input/output, latched safe command, exactly one actual write per tick and native command mapping over all six full traces before reporting the counterfactual first writes.

Input hashes, original first-dose arithmetic, state identity and zero-action parity are exact (shape/dtype/bytes where arrays are involved). Only recomputation of existing decoder/safety/frame arithmetic may use the prior absolute tolerance **2e-15 radians, rtol 0**; report maximum residuals. This tolerance never classifies a transmitted command as zero and never relaxes an efficacy or retention gate. Exact masking means a nonzero decoded coordinate delta and a numerically exact zero reconstructed safe coordinate delta. A zero-dose control is not counted as a masked perturbation.

## Fixed output and decision

Write all 32 rows with state/branch identities, requested/applied action increments, action clipping, full 18-coordinate decoded logical deltas, safe logical deltas and native write deltas. Report exact masked-coordinate indicators and nonzero-coordinate counts. Report decoded and safe logical-space L2 norms and their ratio; the zero-dose ratio is undefined (`null`). These angular norms describe the command representation, not mechanical energy, work, foot motion or comparable joint leverage. No threshold on these ratios selects a next controller or a favorable state. No fitted delays, weights, gains or new templates.

The sole decision is `VALID_ALGEBRAIC_FIRST_WRITE_AUDIT` if the fixed inventory, all 6,020 baseline checks, all 32 original first-dose comparisons and all eight zero controls pass. A missing input, mismatch or assumption failure means `INVALID_OFFLINE_AUDIT`; preserve the partial output and exact failure, stop, and do not replace a state or silently widen a tolerance. Unit tests must pass and root must review the implementation before the diagnostic runs. Expected cost is seconds of local NumPy work and no GPU or simulator usage.

A zero first-write difference does **not** show that the remaining four pulse ticks were masked. A nonzero first-write difference shows transmission through safety and the write mapping, **not** through delayed servo-profile maturation or the plant. Neither outcome proves a cause of yaw failure or a useful correction. The original 5 mrad burst gate, both-signed retention (complete 80 ticks, all walking, zero termination, positive baseline forward motion, >=.9 forward ratio, <=1.25 loaded-slip ratio, <=baseline+3° relative tilt), straight retention and continuous-joystick qualification remain unchanged. No new efficacy decision, law or follow-up is authorized by this result.
