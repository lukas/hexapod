# Root pytest config: put the prototype root on sys.path so both bare
# `uv run pytest ...` and `uv run python -m pytest ...` resolve the
# packages (rl_move, hexapod_core, motor_setup, linux_control helpers)
# identically. Suite-wide env pins live in rl_move/tests/conftest.py.
[pytest]
pythonpath = .
