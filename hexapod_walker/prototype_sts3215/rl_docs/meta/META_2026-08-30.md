# Meta-analysis — 2026-08-30 (~09:20-10:00)

## 1. Progress toward the goal (last ~24h: 53 cycles, 20.4h agent time, ~$319 agent spend, ~30 GPU runs)

- **standwalk (goal-critical): REAL progress.** 12 cycles/4.1h produced the first
  mesh/100Hz all-heading walker to pass the formal 60s stress_mix joygate outright
  (singleframe-acq1-stdanneal: 0 falls/24, slip 2.35, windowed course_err in-band)
  AND confirmed it distill-compatible. Stage-2 distillation then shipped a broken
  base (walk clone never moved: net disp 0.02m/30s) and 2 RL canaries were funded on
  it before the defect was caught — but the root-cause + a quick_probe net-disp
  WARNING landed same-day. Next gap is exactly right: fix the distillation recipe,
  verify net_disp_m, THEN fund RL.
- **walkcurr: zero gate movement for 28 cycles/12.6h + most GPU-hours; ~25 FAILs,
  all the same static-quiver basin.** Much was operator-ordered (10-arm population
  sweep) and each arm honestly closed a pre-registered lever — but information per
  run collapsed after ~the 3rd identical basin read. Every operator-named lever is
  now closed except SAC tilt5 x4 (finishing within the hour, 18-19.5M/20M). STATUS
  already pre-commits the closure path: 4/4 FAIL -> park [operator], no mechanism #18.
  Queue verdict: correctly aimed; the failure mode to avoid tomorrow is dose-grid
  archaeology on a refuted mechanism.
- **joystick/amp/cpg:** DONE-or-[operator]; correctly consuming ~nothing.
- Queue state OK: backlog empty is legitimate (standwalk's next item is CPU/code
  work named in its STATUS; walkcurr's is the batched tilt5 read).

## 2/3. Bottlenecks measured, and what was changed

Measured sinks: (a) 2 false-SUSPECT checkup cycles on healthy end-of-run canaries
(anchor14coef1-canary{,-s1} — W&B step lag + MJX log silence + eval work in mp
children the single-PID CPU probe can't see); same class burned deep-model cycles
08-25/26/27 per code comments. (b) Duplicate-verdict races: decleg-sv-s2/s6-b100m
each verdicted by TWO cycles with conflicting tallies; grad-s0 raced too (~1 wasted
cycle, ~$5-11). (c) Heavy-cycle anatomy (052839: 178 turns/$11/58min; 060928: 236
turns/$15): ~50% of turns are re-reads/greps of giant STATUS journals (standwalk was
7706 lines, walkcurr 2796), 10-18 hand-rolled python3-c ledger parses, ~10 sleep-poll
turns babysitting pytest/training against the "never wait" rule. (d) Median cycle
$4.06/25min is healthy; tiering works (44 sonnet / 9 fable).

### Changes MADE (all smoke-tested; snapshot meta-2026-08-30)
- `launch_run.py` (+~45 lines): `_pod_run_cputime` — checkup CPU proof-of-work now
  sums the trainer's whole descendant tree (spawn mp workers/ffmpeg), fixing the (a)
  false-SUSPECT class at its root; validated live on train-8 (27 children, 3.18M vs
  1.83M centisec, 0.4s scan; bogus-run fallback OK). Also un-confused the checkup
  print default ('smoke' -> 'n/a').
- `ops.sh verdict` (+13 lines): first-verdict-wins race guard — a second writer gets
  REFUSED with the existing verdict text (FORCE=1 to override). Kills (b) dupes and
  tells the losing cycle instantly to stop. Tested against a real verdicted run.
- `ops.sh entry <run> [field]` (+6 lines): one-field ledger reads (pod/status/
  extra_args...), replacing the hand-rolled python3-c parses in (c). Tested.
- **STATUS journal trim (biggest token fix): walkcurr STATUS 2796 -> 481 lines,
  standwalk STATUS 7706 -> 1711 lines.** Old journal entries + standwalk's
  SUPERSEDED 08-27-era "## Now"/"## Next" (a live footgun — stale actionable items)
  moved VERBATIM to `archive/{walkcurr,standwalk}_STATUS_journal_2026-08-30_trim.md`
  with pointer notes. Nothing reworded; Goal/rules/ladder/current entries kept.
  Anti-bloat accounting: only shrinkage in standing docs; new files are the two
  verbatim archives + this report.

### Recommended, NOT made (and why)
- Watcher restart NOT required: checkups run `launch_run.py checkup` as a
  subprocess, so the tree-CPU fix is live for all future checkups without touching
  the running watcher. No action-list restart line needed.
- SAC `--init-from` checkpoint continuation: only worth building if a tilt5 arm
  escapes (walkcurr STATUS pre-commits this); don't build speculatively.
- Batch the 4 tilt5 reads into one cycle: watcher already batches co-finished runs;
  they're within 1.5M steps of each other so this likely happens naturally.
- Trim joystick/amp STATUS (2573/4065 lines): DONE-track docs, rarely deep-read;
  churn risk > token savings tonight.
- Operator-kick cycles ran on fable at $17-20 each (2 of top-4 costs). Whether kicks
  deserve the deep tier is the operator's call — flagged, not changed.

## Open questions for the operator
- walkcurr terminal fork (already filed in its STATUS/OPERATOR_QUESTIONS): if tilt5
  reads 4/4 FAIL — new mechanism idea, or park rung-1 and concentrate on standwalk?
- Confirm kick-cycle model tier preference (above).
