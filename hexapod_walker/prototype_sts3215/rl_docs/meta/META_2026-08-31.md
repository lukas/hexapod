# Meta-analysis — 2026-08-31 (~09:20-10:15)

## 1. Progress (last ~24h: 44 cycles, 17.6h agent time, ~$224, ~30 GPU runs)

- **standwalk (goal-critical): honest negative results, wrong search level.**
  The scripted all-heading teacher line delivered real assets early (bc1_std25
  clone turns wz~0.23 both signs pre-RL; acq12m 2/2 PASS; anchor14coef1 canary
  2/2 PASS). Then the turn-authority campaign burned ~16 canaries / 8 mechanism
  classes (anchor dose x3, turn-skip, isolate-update, ent-coef, log_std x2,
  yawscale in flight) — every one FAIL with wz_med <0.03, on a base whose
  pre-RL probe already read -0.04 one-sided / +0.00 frozen (RL_LOG 08-31
  02:35). Asking PPO to DISCOVER turning was refuted 8 ways while the actual
  defect — the dual distillation destroys the teacher's symmetric turn
  authority — sat un-audited. Each class was cleanly refuted (good hygiene,
  incl. the new probe_turn_authority + probe_yaw_credit tools, the latter
  showing the critic carries no wz-forward signal), but ~12 cycles of
  mechanism archaeology repeated last night's warned failure mode one level
  up. FIXED: standwalk STATUS now leads with a priority reorder — audit the
  distill turn path (sign/coverage of the wz channel), gate every dual
  distillation on pre-RL probe_turn_authority >=0.15 both signs, and only
  fund RL as turn RETENTION. No 9th RL class on the current base.
- **walkcurr: RETIRED cleanly** — litrep-box s0/s1 (150M each) landed the
  pre-committed park-stand read; ruling (d) executed, tracks.json updated.
  Correct closure, no wasted follow-on.
- **joystick/amp/cpg/todaypolicy:** DONE-or-maintenance, consumed ~nothing. OK.
- Queue state: backlog empty is legitimate; next work is the CPU-side distill
  audit named in standwalk STATUS ##Next, then gated re-distill + RL.

## 2/3. Bottlenecks measured, changes made

Measured: median cycle 20.3min/~$5.1; 112 tool-timeout mentions across 44
cycles; 281 REFUSED mentions; 42 sleep-polls; 3 duplicate-triage cycles
(turnpay-s1 01:12, turnpay 04:11, entboost 06:51) each re-deriving evidence
for an already-verdicted run before hitting the 08-30 race guard; 1 cycle
lost its logline to a wrong path; heaviest cycle $42/112min (fable dig-in).

- **Root-caused the fleet-wide slowness: 313,722 zombie git processes on the
  controller** (PID 1 = `sleep infinity` never reaps; status_server's 60s
  `git fetch` -> `gc --auto` self-detach -> orphan -> permanent zombie, ~70/hr
  for ~22 days; 50 packs >= autoPackLimit made gc fire on EVERY fetch). This
  is why /proc scans blew the 60s kexec timeout (`launch_run.py status`
  printed the controller "unreachable" every call) and shell latency was
  inflated. Fixed at source: `gc.autoDetach=false` + `maintenance.autoDetach
  =false` in /workspace/hexapod + one-time foreground `git gc` (0 loose, 3
  packs); verified fetch now leaves zero zombies. Existing zombies need a pod
  restart — filed q_20260831T1000Z, not urgent.
- `launch_run.py` (+5 net lines): `_TRAINER_SCAN_SCRIPT` rewritten fork-free
  (bash `mapfile` instead of a `tr` fork per PID — 313k forks was the 70s).
  5.6s on the bloated pod, 12x faster; `status` 76s+ -> 16s total; behavior
  identical (test_pod_trainers_scan 3/3 green, live-checked train-1 +
  controller). Live for watcher checkups without restart (subprocess calls).
- `ops.sh review` (+8 lines): loud "ALREADY VERDICTED — do not re-triage"
  banner with the existing verdict text as the FIRST output, so the 3
  dup-triage cycles' class ends after one command instead of a full re-read.
- RL_LOG repair: the 08-30 16:2x scripted-allhead logline had been written to
  a stray `rl_move/orchestrator/RL_LOG.md` (never reached the real log);
  merged chronologically, stray file deleted.
- **standwalk STATUS 3029 -> 2237 lines**: 08-29 journal entries (827 lines)
  moved VERBATIM to the existing archive file; added the 37-line "## Next"
  priority reorder (the question-1 fix). Net -792.

## Anti-bloat accounting
Standing docs: standwalk STATUS 3029->2237 (shorter). No other standing doc
grew (RL_LOG +1 recovered history line, stray dup file -1). New files: this
report only. All changes are code/helpers, not new process steps.

## Recommended, NOT made (and why)
- Controller pod restart to reap the 313k zombies: operator infra action
  (q_20260831T1000Z); everything is functional post-fix, so not blocking.
- Pre-RL turn gate as launcher-enforced code: left as a STATUS acceptance
  bar; enforcing distillation quality in launch_run.py would couple the
  launcher to track-specific evals — revisit only if the bar gets skipped.
- Fable dig-in cost cap ($42/112min outlier 08-30 23:16): the cycle did land
  the root-cause; no evidence a cap helps more than it costs. Watch a night.

## Open questions for the operator
- q_20260831T1000Z (controller restart, above).
- Confirm the standwalk reorder reading: distill-side turn repair before any
  9th RL mechanism class — matches the 08-21 ruling's "fix alignment, not
  more doses" spirit; encoded in standwalk STATUS ##Next.
