# Meta-analysis — 2026-09-01 (~09:10-10:00)

## 1. Progress (last ~24h: 39 cycles, 13.9h agent time, $150.58, ~14 GPU runs)

- **standwalk (sole active track): real, disciplined progress — one campaign
  question fully closed.** The mirror-augment BC base (first turn-capable
  pre-RL base, 08-31 13:1x) held through a 2-seed RETENTION canary PASS, then
  the erosion campaign closed the entire update-size-constraint family with a
  proper matched-drift control (n=2): freeze only DELAYS erosion; total
  actor-training-steps drives it (09-01 09:07). Reward pricing (1x/5x),
  k_yaw_still, turn density, value-warmup — all cleanly refuted, each with
  its own gate. Durable ceiling: wz pos ~0.075-0.09 vs the 0.10 PASS bar.
  Next lever (reward-decomposed critic) is correctly scoped but UNBUILT, so
  12/12 pods sit free — legitimate (remaining work is CPU-side code) but the
  first post-meta cycle must start building, not re-verify. Judgment lesson,
  not process: the 0.01 kl-rollback arm (38M) contradicted the campaign's own
  measurement (realized KL 0.08-0.15) and predictably rejected every actor
  update — an accidental critic-only run now flagged DIG-IN. A 1M canary or
  a 5-line pre-check against the measured KL floor would have saved the arm.
  Also: dose ladder 0.05→0.02→0.01 was serialized over 3 cycles; the
  matched-drift control could have shipped with the first klroll pair.
- **joystick/amp/cpg/todaypolicy/walkcurr:** DONE-or-maintenance/delivered/
  retired; consumed ~nothing. Correct.
- **Queue fixed:** standwalk STATUS "## Next" was stale (08-31 era, buried at
  line 818 of a 3680-line file). Rewrote it: (1) klrolltight2 dig-in fork,
  (2) build+canary the reward-decomposed critic (batch the A/B pair),
  (3) pre-RL turn bar stands, (4) closed-lines list so no cycle refunds them.

## 2/3. Bottlenecks measured, changes made

Measured: median cycle $3.07/69 turns/15.2min (vs $5.1/20.3min last night —
33% cheaper day: $150 vs $224). 6-7 SUSPECT checkup cycles ALL root-caused to
the same cause: prestage evals (often for already-verdicted runs) saturating
a 24-28 core cgroup shared with a live/next trainer (08-31 13:5x, 23:07;
09-01 01:20, 03:49, 03:5x, 06:3x, 08:5x) — each burned an agent cycle plus
trainer fps. 1 config-twin double-launch by concurrent cycles (klroll vs
klrollctrl, killed at 1M). 3 ALREADY-VERDICTED banner hits exited fast
(yesterday's review-banner fix works — dup triage essays gone). Sleep-polling
modest (~45min total across sampled logs). standwalk STATUS regrew 2237→3680
lines in one day (+64%), taxing every reader.

- **pod_eval.py (+10 lines, 4 spawn sites):** all prestage evals
  (eval_checkpoint/session/mixed_session/joygate) now spawn under
  `nice -n 19` — spawn-time nice inherits to every worker thread/child
  (verified on train-0), so trainers win contention while idle-pod evals run
  full speed. Kills the whole recurring SUSPECT class at source; `ops.sh
  niceevals` stays for legacy procs. Live without watcher restart
  (subprocess-invoked).
- **launch_run.py (+~55 lines):** launch-time TWIN GUARD — refuses a
  non-smoke launch whose normalized train args+steps (minus --out-name/
  --notes) exactly match a RUNNING run's; `--allow-twin` override on
  launch+respec. Verified against the real incident pair (collides), seed
  twins and unrelated runs (don't). 53/53 launcher tests green.
- **standwalk STATUS 3680 → 223 lines (-3457):** journal 08-30→09-01 07:4x
  moved VERBATIM to `archive/standwalk_STATUS_journal_2026-09-01_trim.md`;
  fresh 32-line "## Next (meta 09-01)"; Goal/Stages/Landmines kept.

## Anti-bloat accounting
Standing docs: standwalk STATUS 3680→223 (shorter). No other standing doc
grew; no new rules/steps. New files: this report + the verbatim journal
archive (established trim convention, net -3457 process lines).

## Recommended, NOT made (and why)
- kl-rollback partial-step-scale-down (vs reject-and-skip): waits on the
  flagged klrolltight2 dig-in fork — building it now would preempt the
  decision that dig-in exists to make.
- Automated STATUS journal rotation: two manual meta trims in two nights; if
  it regrows past ~1500 lines again, add an `ops.sh statustrim` helper —
  third occurrence proves the need, not the second.
- Controller pod restart to reap the 313k pre-existing zombies: still
  operator-owned (q_20260831T1000Z); fleet functional.

## Open questions for the operator
- q_20260831T1000Z (controller restart) still open, not blocking.
- standwalk is one unbuilt tool away from its decisive critic A/B; if that
  pair also fails, the honest next fork is architecture (command-conditioned
  critic / separate turn head) vs accepting composed control (todaypolicy
  bundle) as the deliverable while research continues — flagging early.
