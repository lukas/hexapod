# Meta-analysis — 2026-09-02 (~09:00-09:30)

## ACTION LIST (first line = watcher restart to activate tonight's change)
1. `bash rl_move/orchestrator/restart_watcher.sh`  (picks up pending-eval registry)
2. First post-meta cycle: standwalk STATUS "Next" items 1+2 (cap=2.9 batch + re-probes).

## 1. Progress (last ~24h: 36 cycles, 10.3h agent time, $131.74, ~12 GPU runs)

- **standwalk (sole active track): the best day of the campaign — a real root
  cause found, fixed, and CONFIRMED.** Chain: yaw-credit critic built + dose-
  bracketed (0.15 PASS at canary, FAIL at 0.5/2.0, PARTIAL at 38M) → session
  gate read exposed 22-27/32 over-current terminations → duration-mismatch
  quartet + switch-jump instrumentation → per-joint trace LOCALIZED it (femur
  pinned at ~2.64A vs 2.5A cap, mid-rise) → mass refuted (legacy 2.1kg pins at
  the SAME 2.64A) → frame-blend refuted (n=2) → cap=2.9 session read (landed
  06:52-08:3x, read by this meta session): **32/32 zero-term, dr0 AND own-DR**
  vs control's 24/32. The DONE gate's zero-falls bar is now MET by the control
  checkpoint; remaining gaps are direction_err_med 46.8° (steering) and slip
  3.09 vs ~2.9 (marginal). Big implication queued: every prior turn-authority
  arm trained/evaled under cap 2.5 with spurious terminations — the closed
  0.075-0.09 wz ceiling verdicts are suspect; read-only re-probes queued.
- **joystick/amp/cpg/todaypolicy/walkcurr:** green-maintenance/delivered/
  retired; consumed ~nothing. Correct.
- **Queue fixed:** standwalk Next rewritten around the confirmed lever:
  (1) cap=2.9 acquisition batch (+seed twin, ONE cycle), (2) zero-compute
  re-probe of closed champions under cap=2.9, (3) steering gap as the next
  science, judged on the literal 60s session read, not the short probe.

## 2/3. Bottlenecks measured, changes made

Measured: $131.74/day (vs $150.58 yesterday), median cycle $2.42/15.2min.
21/36 cycles were idle kicks ($68.71, 52%) — but the two big ones ($17.6)
did the day's decisive root-cause work; the real waste was structural:
the watcher is blind to on-pod evals, so (a) ~9 no-op kick cycles ($7-10)
hand-polled in-flight eval panels via kubectl, (b) landed results sat
unread up to hours awaiting the next backoff-delayed kick (the decisive
cap29 verdict itself waited ~30-60min and was only read because meta
happened to run), (c) one cycle sleep-polled a panel for 11min live.
Yesterday's fixes held: no-op kicks now cost $0.6-1.3 (banner works),
zero SUSPECT-checkup cycles from eval CPU contention (nice-19 works),
one twin-guard REFUSED visible in the ledger doing its job.

- **watch_loop.py (+75 lines, code): pending-eval registry.** Cycles register
  detached on-pod evals (`ops.sh evalpending add <pod> <file> <label>`); the
  idle loop HOLDS idle kicks while any is in flight (no more hand-polling
  no-op cycles) and spawns a labeled `evalready` cycle the MOMENT the result
  file lands (no more hours-unread verdicts). 8h TTL so a dead eval can't
  deadlock kicks. Smoke-tested live against train-1: ready-detect+prune,
  in-flight retention, TTL expiry all verified. Needs the restart above.
- **ops.sh (+28 lines): `evalpending` subcommand** + `donegatecmd` now prints
  the registration command instead of the blocking `waitlog` sleep-poll
  suggestion (deletes a wait step from the standard flow).
- **standwalk STATUS 157 → 120 lines (-37):** superseded Updates archived
  verbatim (`archive/standwalk_STATUS_journal_2026-09-02d_trim.md`); fresh
  decisive-cap29 Update + 4-item Next; back at the 120 budget.

## Anti-bloat accounting
Standing docs: standwalk STATUS 157→120 (shorter); no other standing doc
touched. New standing rules/steps: none (one wait-step DELETED, one step
automated). New files: pending_evals.json (machine state), one verbatim
archive (established convention), this report.

## Recommended, NOT made (and why)
- **Shard eval_done_gate_session panels across free pods** (n=32 panel =
  ~3h serial on one pod's CPU while 11 GPUs idle; it was the day's critical
  path twice). Real build + merge semantics on the campaign's gate
  instrument mid-campaign — propose as a normal-cycle CODE item only if
  panel latency stays on the critical path after the pending-eval registry
  removes the polling cost.
- **statustrim helper:** two manual meta trims became three (this one) —
  by the 09-01 report's own rule the third occurrence proves the need, but
  tonight's trim was bundled with a content rewrite; next pure-growth trim
  should build `ops.sh statustrim` instead.

## Open questions for the operator
- q_20260831T1000Z (controller restart for 313k zombies) still open; also
  train-0 is Pending/unreachable since its 09-01 idle OOMKill — fleet is at
  11/12 slots, fine for current load, flag if a 12-arm batch is ever needed.
