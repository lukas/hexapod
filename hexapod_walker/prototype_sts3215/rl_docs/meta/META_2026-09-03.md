# Meta-analysis — 2026-09-03 (~09:40-10:00)

## ACTION LIST
1. `nohup bash rl_move/orchestrator/restart_watcher.sh >> /workspace/restart_watcher.log 2>&1 &`
   — REQUIRED: activates BOTH the 09-02 pending-eval registry (built last
   night, NEVER went live — see bottleneck #1) and tonight's autorestart
   hook. Also planted as standwalk STATUS Next item 0 so a normal cycle
   executes it even if nobody reads this report (that is what failed 09-02).
2. Then: read `resamplematch-canary{,-s1}` (in flight, train-1/2) per
   standwalk STATUS Next item 1; any new donegate read uses the sharded
   command `ops.sh donegatecmd` now prints (~2h instead of ~14h).

## 1. Progress (last 24h: 27 cycles, ~11.6h agent time, $96 measured / ~$110 est, 4 GPU runs + 1 relaunch)

- **standwalk (sole active track): strong, honest progress on BOTH fronts.**
  Science: cap29 zero-falls lever compounded with the walk-log_std-anneal fix
  (dose-bracketed 2-seed canary -> 38M acq) into a 2-seed PASS on the real
  video-bearing flat-only donegate session — dir_err 44.6/43.6deg + slip
  2.82/2.94 BEAT the 46.8/3.09 baseline, zero falls in 256 eps. Track gap is
  now steering only (~44 vs ~40deg; slip at the 2.9 cap edge); the queue's
  one open arm (resamplematch diet-match canary, 2 seeds) aims exactly at it.
  Infra: the operator's 09-02 joint-frame-v2 merge fallout was caught and
  repaired by kick cycles — deleted spec files restored, 3 classes of stale
  plant-literal bugs fixed with measured A/Bs, a production trans_drag
  deadband bug fixed, and a fleet-wide silent warm-start blocker
  (checkpoint contract stamp) found + 1131 checkpoints backfilled. The five
  $9-17 cycles were these digs; they were the day's best-spent dollars.
- **joystick/amp/cpg/todaypolicy/walkcurr:** green-maintenance/delivered/
  retired; consumed ~nothing. Correct.
- **Queue verdict: correctly aimed.** No misprioritization found; no queue
  edits needed beyond the restart order above.

## 2/3. Bottlenecks measured, changes MADE

Measured: median cycle $2.58/15min; 5 costliest ($9.2-17.4, 142-214 turns)
= the merge-fallout digs (productive). The two structural wastes:

1. **Last night's watcher fix never activated.** Watcher process started
   Aug 29; watch_loop.py modified Sep 2 09:08. The restart lived only in
   META_2026-09-02.md's action list, which no cycle reads. Cost: ~8
   hand-polling idle kicks (~$6-8) + stale pending_evals.json entries.
   FIX (watch_loop.py +41): `maybe_autorestart_on_new_code()` — watcher
   self-triggers the sanctioned restart_watcher.sh when its own on-disk
   code hash changes (parse-checked, pgrep-guarded, script owns all cycle-
   draining safety). Unit-smoked both branches. Needs the one manual
   restart above to go live; after that, watcher edits self-deploy forever.
2. **14h serial eval panel on the track's critical path.** Item-0 donegate
   session (launched 18:29, landed ~08:30) ran ~32 eps serially per pod
   while 9-11 GPU pods idled. 09-02's meta deferred sharding "unless it
   stays on the critical path" — it did, the very next panel.
   FIX (eval_done_gate_session.py 132->181): `--shards K`/`--jobs` —
   pass x shard grid of resume-safe eval_checkpoint subprocesses, disjoint
   seed streams, per-episode rows unioned into the SAME aggregate_session
   (gate semantics unchanged; verified by synthetic end-to-end smoke +
   9/9 existing bank). Default 1 = bit-exact serial. ops.sh donegatecmd
   (+3 printed lines) now emits `--shards 8` (~8x): panels episode-match
   only same-layout reads — first sharded comparison should re-read its
   baseline sharded (cheap now).
- pending_evals.json: cleared 2 stale landed entries (machine state).
- standwalk STATUS 120 -> 119 (restart order added, archive para trimmed).

## Anti-bloat accounting
Standing docs: standwalk STATUS 120->119 (shorter); no other standing doc
touched. New standing files/rules/steps: none (one manual step — watcher
restart choreography — permanently AUTOMATED; one 14h wait cut ~8x).

## Recommended, NOT made (and why)
- Backfill `--shards` into eval_mixed_session/bulk evals: same pattern
  applies, but neither is on the critical path now; do it when one is.
- Investigate 5 cycles with missing cost events in cycles.json (likely
  WRAPUP kills) — cosmetic, cost accounting only.

## Open questions for the operator
- q_20260831T1000Z (controller zombie PIDs) still open; train-0 still
  Pending/unreachable (fleet at 11/12 — fine for current 2-arm load).
- safety.max_current_a 2.5->2.9 assume-and-go (q_20260902T0530Z) is now
  load-bearing in the champion lineage; bless or veto when convenient.
