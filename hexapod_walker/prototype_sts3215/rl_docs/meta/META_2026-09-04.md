# Meta-analysis — 2026-09-04 (~09:15-10:00)

## ACTION LIST (for the first resuming cycles)
1. Triage the finished transtress/cont quartet — but read standwalk
   STATUS Next item 2 FIRST: `cont{,-s1}` doubles as the retroactive
   matched-continuation control for the ENTIRE steering-lever FAIL wall.
2. selomegaboost4p0-s1 verdict: checkpoint is on the controller (07:12);
   its prestage evals died with old train-4 — `ops.sh podeval` on a live pod.
3. When train-4 goes Running (recreated tonight, Pending on node CPU):
   `bootstrap_train_pod.sh hexapod-mjx-train-4` + `pod_torch_capability.py
   install` + `snapshot.sh --sync` before first use.

## 1. Progress (24h: 44 cycles, 19.1h agent, $240; ~30 GPU canaries, 29 verdicted)
- **standwalk (sole active track).** Two honest wins: the 09-03 morning
  log_std-anneal 2-seed donegate PASS (dir 44.6/43.6deg, slip 2.82/2.94,
  zero falls — new reference band), and same-day execution of the 09-04
  operator directive (over_current audit with counterfactual replay,
  eval_cmd_stress promotion suite, smoothness telemetry, transtress
  canary pair + matched controls). Zero-training discipline was
  excellent: 3 probe tools built, 4+ lever ideas refuted for $0.
- **The flag: ~26 steering canaries across 6 lever families (combskip,
  omegaboost, yawboost, yawarm, combdose, triplecore, selomegaboost)
  ALL failed the same 10% pure-turn-regression cap — and ALL were
  scored against FROZEN parent checkpoints.** The only continuation
  control (dualcontinue-noyawcredit) also dropped training mechanisms,
  so "plain 2M continuation alone breaches the cap" is unfalsified. If
  cont{,-s1} (finished, prestaged) breaches it, the axis's 24h of GPU
  + deep-cycle spend measured continuation drift, not lever harm.
  Encoded as STATUS Next item 2 (mandatory read order). Method lesson:
  on a fine-tune grid, the no-lever matched continuation is arm #1,
  not arm #27.
- **Queue verdict: correctly aimed** (quartet triage + confound read =
  top item; operator's universal-command branch is TOP). Other 5 tracks
  correctly dormant. No queue edits beyond the STATUS read-order.

## 2/3. Bottlenecks measured, changes MADE
Measured: median cycle $4.37/23min (up from 09-03's $2.58/15min —
driven by 11 kick cycles, several $10-18, mostly productive lever
digs); 31 triage / 11 kick / 2 findings; model tiering 40 sonnet : 5
fable. Watcher code-change self-restart (yesterday's fix) confirmed
LIVE (restarted itself 2 min after a 07:05 edit — no manual step).
1. **Recurring launch snag automated** (hit 3 cycles on 09-03, ~10-15
   min manual pod surgery each): `linux_control/vision_ui` became a
   symlink; tar can't extract a symlink over the stale pod dir. FIX:
   snapshot.sh --sync now pre-removes exactly the tar's symlink
   members when the pod copy is a real dir (+12 lines; tested via
   local repro + live sync; NOT --recursive-unlink, which my test
   showed deletes pod-side logs/).
2. **Eval-vs-trainer CPU starvation remedy automated**: nice-19 proven
   insufficient 09-04 00:01 (4 co-located eval trees starved a trainer
   to death; manual taskset fixed it instantly). FIX: `ops.sh pinevals
   <pod>` (+39 lines) — one command pins all eval trees to the last 6
   allowed cores; checkup-triggered by design (deliberately NOT done at
   spawn: it would slow every idle-pod prestage ~4x).
3. **Fleet repair**: train-4 (OOMKilled 08:06) deleted + recreated from
   the fixed 4Gi-dshm scaleout spec per CAPACITY.md; Pending — g142d86
   is 98% CPU-requested by other tenants. NOTE: g131eec is now
   SchedulingDisabled (train-2/3 still run); 2 of 4 nodes cordoned.
4. standwalk STATUS 129 -> 125 (cont-as-control read order + fresh
   fleet note added; stale 08-3x banner trimmed to a pointer).

## Anti-bloat accounting
Standing docs: standwalk STATUS 129->125 (shorter); no other standing
doc touched. New standing files/rules: none. ops.sh/snapshot.sh growth
is helper CODE replacing repeated manual work (2 manual procedures
automated, 0 new required steps).

## Recommended, NOT made (and why)
- --shards backfill for eval_mixed_session/bulk: still off critical
  path. 14 REFUSED/24h: normal concurrency traffic, no code needed.
- A "control-first grid" rule in RESEARCH_RULES: would grow a standing
  doc; STATUS item 2 enforces it where it matters now — revisit only if
  the pattern recurs on a fresh axis.

## Open questions for the operator
- g131eec SchedulingDisabled + g142d86 98%-requested by other tenants:
  is the fleet shrinking? train-4 stays Pending until CPU frees.
- q_20260831T1000Z (zombie tmux client, Aug 9) still open, harmless.
- safety.max_current_a rider question from 09-03 still open; tonight's
  audit (cap29's 2.9 can never trip vs the 2.64 A rail) sharpens it.
