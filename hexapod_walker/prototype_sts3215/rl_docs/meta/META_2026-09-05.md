# META 2026-09-05 (nightly meta-analysis, 09:37Z)

## Q1 — Progress toward the goal (last ~24h: RL_LOG 1216–1257, ledger, STATUS docs)

- **walkcurr (REOPENED, now PRIMARY per operator 09-05 order): the day's real progress.**
  Easy-sim teacher-free pilot built with pre-launch proof (13/13 new bank tests, windfall
  probe, action-box fix), 4/4 2M canaries CANARY PASS, then full-fleet fill: 11/11 ready
  slots training 40M arms (base/sde/halfgrav/sdehalfgrav grid + own-ckpt continuations),
  train-3/-5 already at the 40M budget. Queue is aimed at the operator's named largest gap.
- **standwalk: honest but expensive closure.** ~20+ canary verdicts closed the ENTIRE
  teacher-side steering-lever family (static doses, selective omega boost 4/4, phase-scheduled
  multi-teacher 4/4); the comparator-noise dig-in (n=4 zero-lever continuation band) exposed
  that the earlier "FAIL wall" was partly frozen-parent scoring — good methodology catch.
  mlcontprice8 DONE-gate read: zero falls but slip 3.70 vs 2.82–2.94 band. Track's Next list
  is now fully closed; no runnable GPU item (correct while walkcurr owns the fleet).
- **todaypolicy/hardware_delivery:** operator-fed CPU verification work (envelope governor,
  filter-lag replication, noise screen) executed cleanly, zero GPU. Others: DONE/RETIRED.
- **Idle-fleet miss:** ~8.4h overnight gap (09-04 18:07 → 09-05 02:30) with 11/12 slots free
  and an empty queue — legitimate under then-closed tracks, but the operator's "not really
  using the hardware" note shows the bar: an empty backlog next to an unmet campaign is the
  thing to prevent. Tonight's fix: backlog restocked 3 → 6 pre-registered 40M arms
  (sde-s3, sdehalfgrav-s2/s3) completing n>=4 seeds per family, so the drain survives the
  overnight wave of 40M finishes without waiting for a decision cycle.

## Q2 — Analysis bottlenecks (measured)

- 28 completed cycles / $168 / 11.4h agent-time in 24h; median cycle ~15 min, ~80 tool calls.
- **#1 wall-clock sink: hand-rolled remote sleep-poll loops.** Cycle 20260904T152831 (4297s)
  spent ~50 min in 7 hand-written `sleep 60; kubectl exec … tail` loops waiting for an on-pod
  eval, despite `evalpending` existing. FIXED: `ops.sh podwaitlog <pod> <file> <regex> [t]`
  (one helper replaces the pattern; comment points to evalpending+exit as the better move).
- **train-6 Pending re-derived by ≥3 cycles** (and one Codex audit) before the CPU-full root
  cause stuck. FIXED: capacity.py now prints the scheduler's own Unschedulable message next to
  every Pending pod (fb_20260905T092735 "record the blocker" executed as automation, not prose).
- Verdict races: none found — the two 08:01 simultaneous operator-kicks did disjoint work
  (walkcurr reopen vs delivery verify). Tool-error rate ~4% (median 3 errs/cycle), acceptable.

## Q3 — Agent efficiency

- Model tiering works: sonnet triage cycles avg ~$3.4 (22 cycles), fable dig-in/operator
  cycles avg ~$12.5 (6). The $18.56 fable cycle was the walkcurr rebuild — justified output.
- Batching discipline good this cycle era (4-arm canary grid, 11-arm fleet fill in one cycle).
- The sleep-block guard (tool_use_error on `sleep N; tail`) is already forcing until-loops;
  podwaitlog closes the remaining remote-file loophole.

## Changes MADE tonight

1. `rl_move/orchestrator/capacity.py` (+10/−1): Pending pods show the live scheduler reason
   (e.g. train-6 "Insufficient cpu…"), verified live. No future cycle waits on a phantom slot.
2. `rl_move/orchestrator/ops.sh` (+14, +1 help line): `podwaitlog` helper; `bash -n` clean.
3. Backlog restock via `launch_run.py backlog add` (3 arms, 40M each, pre-registered
   hypotheses/gates, family-canary evidence): sde-s3, sdehalfgrav-s2, sdehalfgrav-s3 —
   completes matched-seed n>=4 per family for the 2x2 sde×gravity comparison.
4. `rl_docs/tracks/walkcurr/STATUS.md` 136 → 136 lines (stale spares list corrected in place).
5. This report (dated exception). No other standing doc touched; no new rules/checklists.

## Recommended, NOT made

- **Watcher restart: none needed** (no watcher code changed).
- Consider a drain-side "low-backlog" warning (backlog < free-slots-expected-overnight) —
  deferred: tonight's restock covers the current wave; add only if starvation recurs.
- standwalk next lever: with every teacher-side steering lever refuted and gait-structure
  axis 2a closed by zero-training probes, the honest remaining moves look like (a) accept
  walk-only DONE-gate hardening (slip 3.70→band) or (b) a non-teacher steering mechanism —
  both compete with walkcurr for GPU. Left for normal cycles + operator weigh-in.

## Open questions for the operator

- walkcurr easy-campaign: when 40M arms pass the acquisition milestone, is the next rung
  physics-hardening (gravity/torque back toward real) or command generalization? STATUS
  implies hardening ladder; a one-line confirmation would pre-register the next grid.
- standwalk: fund (a)/(b) above after the walkcurr grid reads, or park until easy-sim
  transfer evidence exists?
