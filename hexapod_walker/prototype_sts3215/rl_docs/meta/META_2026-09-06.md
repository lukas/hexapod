# META 2026-09-06 (nightly meta-analysis, ~09:1xZ)

## Q1 — Progress toward the goal (last ~24h: RL_LOG 1466–1580, ledger, walkcurr STATUS)

- **walkcurr (PRIMARY, full fleet): genuinely strong day — the campaign crossed to REAL 1g gravity.**
  Cross-gravity transfer (halfgrav champion → abrupt 1.0g) PASSed and matured: medhead-abrupt at 80M
  is 24/24 gait_valid/0 falls; 2nd champion (plainhead-abrupt) confirmed durable at 40M. On top of it,
  ~22 single-axis DR-restore canaries ALL PASS except kick1x (1 real fall — the one binding
  constraint, half-dose brackets clean); torque crutch (3x) removal clean at every dose incl. 1x;
  single-axis ACQ reads (friction/mass/latency…) all PASS; 5 cont40m endurance reads all HOLD.
  This is real movement toward "from-scratch RL walking on real physics".
- **Allocation drift (fixed tonight):** with every single-axis read coming back clean, cycles kept
  funding per-axis 40M ACQ + 80M cont40m confirmations (~20 axes × 40–80M ≈ >1B steps of near-zero
  information) instead of the frontier: composite realism (allaxis / kick-safe composite), the kick
  ladder, no-crutch composite, then contextual DONE rungs. Wrote a QUEUE AIM block at the top of
  walkcurr STATUS (net −8 lines) and queued the pre-announced kick-safe composite canary
  `medhead-dr-allaxiskickhalf1x-c1` to backlog (respec, no launch — meta rule).
- **assistfade: honest rung-1 closure.** 3/3 seeds fail at scale with DECLINING reward (not the
  08-21 rising case) — rung-1 (BC-init, task-only PPO) closed; retreat to rung 2/3 pre-registered.
- **todaypolicy:** robotwalk-turns cont8m-resume1 ACQ FAIL/MISALIGNMENT (course_err worsened with
  reward rising) — yaw-reward audit correctly named as next; no 3rd continuation funded. Good triage.
- **joystick/amp/cpg/standwalk:** correctly parked while the operator's 09-05 full-fleet order holds.
- **Infra day-cost was real but self-repaired:** non-atomic experiments.json writes caused ~360 lost
  entries + 3 orphan/duplicate incidents (all recovered; atomic save + snapshot guard shipped
  e28b9985); respec-steps footgun mislabeled 7 "ACQ" runs as 2M (guard shipped ae26f6b0, r2/r3
  relaunches queued). Both root fixes landed same-day by normal cycles — no meta action needed.

## Q2 — Analysis bottlenecks (measured: 132 cycles, $625, 51.2h agent-time since 09-05 09:00)

- **#1: triage cycles spawning mid-eval.** Watcher's prestage spawn deadman was flat 1500 s while the
  campaign's standard 24-ep video-every=1 gate harness takes 25–40+ min → 57/132 cycles (43%)
  touched "still genuinely computing"/pollreap churn, each ~$4.5 & ~20 min to conclude "wait".
  FIXED: `_prestage_spawn_wait()` scales the deadman with the run's own eval budget (cap 3h);
  sentinel path unchanged. Tests green (5 prestage + 13 refill). **Needs watcher restart.**
- **#2: 80M/cycle cap accounting is manual** — one cycle self-exceeded (120M) and killed a healthy
  4M-step run to correct; backlog items draining mid-cycle are easy to miss. Recommended (not made):
  drain-side step budgeting. Left alone tonight — two incidents, both self-caught.
- **#3: convergent duplicate funding attempts** (same axis picked by 2 cycles, REFUSED noise, ~20
  REFUSED/day) and duplicate RL_LOG verdict lines (2 cycles racing idle unassigned canaries). Cheap;
  ledger guard already refuses the real dupe. No mechanism added.
- Verified the deferred-artifacts adoption's pending one-read confirm: 6/6 post-adoption real
  launches show `checks.defer_final_artifacts=injected`. That loop is closed.

## Q3 — Agent efficiency

- Tiering: sonnet 119 cycles avg $4.50/102 turns; fable 13 avg $6.54/53 turns. Ratio fine; the spend
  problem is cycle COUNT (132/day), not per-cycle cost — fix #1 attacks exactly that (43% of cycles
  had a churn component). Tool-error rate 4.7% (591/12606) — flat vs yesterday, acceptable.
- Batching discipline: good (single-axis grid launched in waves, caps hit, backlog kept fed).
- Ledger bloat: experiments.json = 23 MB / 2531 entries, 699 (28%) are REFUSED noise; .git = 898 MB
  and every snapshot re-commits the full file. Recommended (not made): a tested `launch_run.py
  compact` archiving REFUSED/KILLED-dup entries >48h old — touches duplicate-name checks, needs its
  own tests in a normal cycle, not a solo-night hack.

## Changes MADE tonight

1. `rl_move/orchestrator/watch_loop.py` 1661→1690 (+29): per-run scaled prestage spawn deadman
   (`PRESTAGE_SPAWN_WAIT_CAP_S=10800`, `_prestage_spawn_wait()` cached); kills the 43%-of-cycles
   still-computing churn at its source. py_compile + 18 watcher tests green.
2. `rl_docs/tracks/walkcurr/STATUS.md` 5014→5006 (−8): QUEUE AIM priority block added at top
   (composite > kick ladder > no-crutch > contextual rungs; stop per-axis confirmations); paid for
   by compressing a duplicated footgun description and a stale (already-consumed) refill list.
3. Backlog: queued `cw-...-medhead-dr-allaxiskickhalf1x-c1` (2M canary respec of allaxis1x-c1 with
   dr.walk_kick_prob=0.15) — the kick-safe composite repeat the allaxis launch note pre-registered.
4. This report (dated exception). No standing doc grew; no new rules/files/checklists.

## Action list for normal cycles

1. FIRST: `bash rl_move/orchestrator/restart_watcher.sh` (picks up the spawn-deadman fix).
2. Verdict `allaxis1x-c1` (report likely landed) reading any fall against the known-bad kick dose.
3. Follow QUEUE AIM: composite ACQ on a composite PASS; no new per-axis confirmations.

## Open questions for the operator

- After composite+no-crutch holds at ACQ scale: re-gate the acquisition milestone at full realism,
  or go straight to the contextual DONE rungs (heading changes, slip band) on the realism champion?
- kick axis: is half-dose kick tolerance an acceptable deployment bar, or fund a dedicated
  kick-recovery training rung (the only axis that fell)?
