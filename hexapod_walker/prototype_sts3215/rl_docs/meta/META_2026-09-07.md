# META 2026-09-07 (nightly meta-analysis)

## 1. Progress toward the goal (last ~24h, RL_LOG 1683→1772)

- **walkcurr (operator-ordered primary): strong, well-aimed.** Item(1)
  push-recovery fragility ROOT-CAUSED to the 3x torque crutch via a
  matched-control ablation + 3-seed isolation (crutchoff canaries 3/3
  PASS, 40M ACQ 3/3 PASS, 0 falls, gv 19-22/24). Realism ladder then
  advanced two rungs in one day: 8-way heading widen 3/3 CANARY PASS
  (ACQs in flight), irr-timing 3-seed set launched, speed-range widening
  mechanism (walk_freeprog_cap_dynamic) built+bank-proved+3 canaries
  finished (unread at meta time). Item(4) slip gap honestly CLOSED as a
  boundary: 5 independent mechanism nulls incl. one refuted at bank
  stage with zero GPU, plus a zero-spend DR-realism ablation.
- **assistfade: honest negative grind + real infra.** Rung 3 (residual
  fade) fully closed — 8 arms, 4 lever families, all CANARY FAIL with
  named fingerprints. Rung 4 reverse-handoff built (CPU+MJX vec-env);
  its bank caught 2 real bugs incl. a PRE-EXISTING sharded walk obs-frame
  bug (6 knee obs slots shifted ~0.13 rad in EVERY prior sharded walk
  run) — high-value catch. First rung4 canary launched.
- **todaypolicy: fork-deciding root cause.** Turn-command misalignment
  found (course income integrated a fixed WORLD chord vs BODY-frame
  kernel; turn-refusal out-earned faithful arcing 2094 vs 1960) and
  fixed default-off (walk_course_ref_yaw); yawref ACQ in flight.
  Anchor-dose and cadence-CPG levers both measured closed.
- **cpg/joystick/amp: DONE/maintenance** (period-bound pin refuted, no
  adoption). **standwalk:** design-closed honestly (~24 refuted arms
  with mechanistic root causes); legitimate.
- Queue verdict: aimed at the largest gap the operator named (walkcurr
  realism ladder). No priority fix needed; no filler detected — refill
  cycles correctly declined to invent arms 10+ times.

## 2. Analysis bottlenecks (measured: 101 cycles, $527, ~34 agent-h/24h)

- Split: triage n=51/$260, partial-refill n=31/$160 (30% of spend),
  evalready n=15/$43, operator-kick n=3/$65.
- **#1 sink: defer-final-artifacts blind window.** Watcher triggers on
  W&B 'finished', but the CPU finalizer holds runs 'running' 30-90 min
  after training completes. Agents manually rescued orphaned gate evals
  6+ times (20:01, 21:0x, 22:49, 03:2x, 07:5x, 08:0x); yawref-acq8m sat
  unevaluated 1h+. Cost: ~$25 of cycle spend + 0.5-1.5h result latency
  per wave. FIXED tonight (see below).
- **#2 sink: verdict races / re-confirm cycles.** ≥4 cycles spawned for
  runs already verdicted by concurrent cycles (17:45, 19:41, 20:14,
  20:2x), ~$10-15. Half is spawn-vs-verdict racing (irreducible), half
  is a dedupe bug: ledger_verdicted() only recognized FINISHED/FAILED
  while 1225/1683 verdicted runs use PASS/FAIL/CANARY... FIXED tonight.
- **#3: outlier cycles.** 2 concurrent sonnet triages on the same
  allaxis-nokick family (05:43/05:48: 251+322 turns, $18.54+$21.66)
  partially duplicated a dig. Anomaly: 1-turn $7.91 cycle (16:21) — a
  background-command notification re-billed the full context for "no action".

## 3. Changes MADE (all code; no standing doc touched — all at same line count)

- `rl_move/orchestrator/watch_loop.py` (+102 lines net, incl. comments):
  1. `handoff_watch_worker` thread — polls RUNNING ledger entries'
     on-pod `artifact_handoff/<run>/state.json`; fires prestage (ckpt
     pull + pod evals) the moment phase leaves 'training'. Closes sink #1.
  2. `prestage_finished` idempotent (lock+set); the W&B-finish re-fire
     refreshes ONLY the W&B cache so triage never reads a stale dump.
  3. `ledger_verdicted()` now counts any latest-entry recorded verdict,
     not just FINISHED/FAILED. Closes the mechanical half of sink #2.
- `rl_move/tests/test_watch_loop_refill.py` (+3 tests, 20/20 green with
  test_watch_loop_prestage_timeout.py). Watcher self-deploys via
  maybe_autorestart_on_new_code() after this snapshot — no manual restart.

## 4. Recommended, NOT made

- Capacity-vs-finalizer wrinkle (06:4x): a pod freed early by deferred
  artifacts can host a new trainer while the finalizer still evals —
  bit-exact tests on that pod flaked. Needs a design decision (reserve
  pod until phase=evaluated vs accept contention); defer to operator/day
  cycles — touching capacity accounting at night is riskier than the sink.
- Background-command notification billing ($7.91/turn-0): agent-SDK
  behavior, not fixable in-repo; flagged for operator.

## Open questions for the operator

- OK that early prestage runs gate evals concurrently with the on-pod CPU
  finalizer? Manual practice already did this all day without incident.
