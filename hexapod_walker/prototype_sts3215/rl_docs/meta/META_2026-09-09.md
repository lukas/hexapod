# META 2026-09-09 (nightly meta-analysis)

## 1. Progress toward the goals (last ~24h)
- **walkcurr (rl_only): STRONG — best 24h this track has had.** Root-caused
  the family-wide reward collapse to a UNIT-mismatch dose miscalibration
  (charge=150 tuned on a ~0-1 ratio was reused on seconds-valued excesses,
  20x per-tick ceiling). Recalibrated dose10: duty-ratio + swing-gap CANARY
  PASS n=2 each; new swing-initiation-income mechanism built+tested+PASS n=2.
  Pair combo **ACQ PASS n=2** — the chronic leg0/leg5 sacrifice is repaired
  (duty>=0.22 majority-clears 5/6,4/6 vs baseline 2/6; gait_valid 22/24 vs
  20/24; 0 falls x 48 eps). Income-alone ACQ FAIL n=2 (canary promise washed
  out). Triple-stack canary PASS n=2 -> acq1 (s1 seed-pruned, s0 pending);
  s2 n=3 replicate FINISHED awaiting triage. Also decisively CLOSED the
  DR-breadth/fresh-init line (true zero-DR still doesn't ignite) — the
  warm-start clean lineage is the vehicle; hardness-ramp curriculum is the
  named next build.
- **assistfade (any_means): honest closure, but over-long.** 6/6 mechanism/
  dose variants refuted on rung3's single-leg sacrifice; structural
  blend-schedule fix is the only licensed lever. CRITICISM: the structural
  root cause was documented 09-06; ports 4-6 (swing-gap, swinit, base-dose)
  were predictable FAILs — ~4-6 cycles (~$15-20 + 24M steps) of confirmatory
  spend. QUEUE FIXED: the "design task, not a same-day launch" parking
  violated never-park-on-unbuilt-code; STATUS now names the build as NEXT.
- **Others correct:** joystick/amp/cpg DONE/closed at zero spend; todaypolicy
  physical comparison is Robot-Lab-owned (readiness per hardware_delivery);
  standwalk closed on refuted mechanism (evidence-based, not parked).
- Queue aim is right (leg-repair n=3 + triple-stack acq read next). Watch:
  the efficacy bar is leg-duty repair, not the DONE-gate's speed/heading
  panel — once n=3 lands, the next promotions should re-anchor on the 60s
  contextual-walk panel metrics, and the hardness-ramp curriculum build
  should enter the Next list like assistfade's structural fix did.

## 2/3. Bottlenecks measured (cycles.json + jsonl, 09-08T09 -> 09-09T09)
- 61 cycles, $253.51, 19.2h agent-time; median 933s / 76 turns / $3.00;
  ~83 tool calls/cycle. vs 09-08: 85 cycles / $442.87 (-43% $/day). Tiering
  healthy: 54 sonnet ($2.74-3.03 median), 3 fable (kicks/meta), 5044 tool
  calls total. GPU: 128 ledger rows, ~1.6B budgeted steps, walkcurr 117.
- **#1 Verdict-format bounces: 92 in 48h.** Validator required verdict text
  to BEGIN exactly "CANARY FAIL - MECHANISM"; agents wrote "FAIL-MECHANISM"
  or led with "Result: ...". Each bounce = 1-3 wasted turns. FIXED tonight.
- **#2 Verdict races: 55 "another cycle beat you" in 24h** (yesterday's #3,
  worse) — full triage duplicated, then refused at fan-out. FIXED (tooling):
  ops.sh review now prints a CLAIMED banner when a LIVE cycle (pid alive,
  zombie-filtered, self excluded via process ancestry) owns the run.
- **#3 Dirty-tree launch blocker** (uncommitted status_server backups
  poisoned .code_sha): REFUSED every launch ~19:2x->06:0x incl. watcher
  auto-drain, ~11h queue latency on the plusduty pair; day cycle fixed it
  06:06. 34/128 ledger rows last 24h are REFUSED husks (27%) — mostly this
  plus the respec-without---evidence dance.
- **#4 walkcurr STATUS regrew 1,895 -> 4,287 lines in ONE day** (+2,392;
  yesterday's trim was -9,127). Entries run 60-120 lines each. Not trimmed
  tonight (fresh working context); next trim should archive pre-09-09 once
  the in-flight acq arms close.
- Residual: 370 bare `python3 -c` + 867 grep hand-parses despite yesterday's
  `ops.sh quarters`; habit, not tooling — no prompt line added (anti-bloat).

## Changes MADE (files, line deltas, rationale)
- rl_move/orchestrator/launch_run.py +5: canary-verdict validator normalizes
  separators/accepts the tag in the opening 160 chars — kills bounce class
  while keeping the category requirement. py_compile + 14/14 gate tests green.
- rl_move/orchestrator/ops.sh +35: CLAIMED banner in `review` (live-cycle
  claim via cycles.json, pid ancestry excludes self, zombies filtered).
  bash -n green; live-tested (zombie case correctly silent).
- .state/rl_docs/tracks/assistfade/STATUS.md 2,777 -> 2,777 (net 0): parked
  structural fix rewritten as buildable NEXT item.
- No launches, no watcher restart needed (ops.sh/launch_run.py are read
  fresh per invocation; watcher itself untouched).

## Recommended, NOT made
- Ledger REFUSED-husk hygiene (27% of rows): consider GC or a separate
  refusals log — touching ledger write path at night is too risky.
- cycles.json stale "running" entries (8, zombie/dead pids since 09-06):
  watcher reap gap; harmless now that review filters zombies.
- CURRENT_TRUTHS curation (1,747 lines) — still needs a day cycle (09-08 note).

## Open questions for the operator
- Assistfade rung3 structural rebuild vs. retiring rung3 in favor of
  walkcurr's now-working leg-repair recipe: worth a directional preference?
- q_20260908T0940Z (joystick disposition) still open.
