# META 2026-09-10 (nightly meta-analysis)

## 1. Progress toward the goals (last ~24h)
- **walkcurr (rl_only): honest, decisive closure work + real deliverables.**
  Durability sweep 4/4 HOLD -> pair-only widen8 recipe named default; the
  off-axis-heading front-pair sacrifice was attacked with 8 independent
  mechanisms across all 4 classes (exposure-reweight, price/gain-dose,
  headexplore log_std, self-distill, 100% isolation curriculum, per-heading
  adv-norm, value-calibration diag, decleg per-leg architecture incl. gSDE/
  halfgrav) — ALL closed FAIL with matched-parent panels, byte-identical
  fingerprints (RL_LOG 09-09 12:00..09-10 09:10). Slip floor likewise closed
  (lineage-wide, not introduced by leg-duty charges). Correct discipline
  throughout: cheap 2M canaries, pre-registered gates, no reward-alone claims.
- **Deliverables that moved the parent gates:** interactive joystick sim demo
  now genuinely verified for BOTH goals via headless HTTP capture
  (web_session_drivecapture.py) — chasing it found 3 real bugs (cfg clobber,
  missing 5Hz heartbeat, wall-clock-vs-sim-time phase scheduling) and honest
  reframing of the "reverse" defect (rl_only: genuine, net_disp 0.057;
  any_means: mostly measurement artifact). rl_only physical handoff prepped:
  N-layer/ELU numpy export (parity 2.3e-7) + bundle_rlonly_v1 manifest +
  GO_NOGO. Fleet restored 12->16 slots (new node g13e3e6).
- **assistfade: fully closed** (~36 arms, residual-anneal-gate's both named
  levers FAIL-STILL-STUCK 2/2 each); only rung0 persistent anchor survives.
- **Critique:** from 09-09 ~17:30 to 09-10 ~07:28 the board said "needs a
  genuinely new structural design, declining to rush it" ~10x in a row —
  design work kept deferring to a "dedicated cycle" that never self-selected,
  until 07:28 finally did it (decleg: built->canary->acq->refuted in ~2h,
  exactly how it should look). FIXED mechanically (see #2) and in the queue:
  walkcurr WAITING-ON was stale (pointed at the closed 09-05 pilot); now
  names the design-note-first reopening rule + Robot Lab trial as live items.
- **Both goals' critical path is now Robot Lab physical trials**, not cloud
  GPU: bundles exist for both (todaypolicy mlpsf-tuck-v1; bundle_rlonly_v1
  with derate warning). Cloud queue empty is CORRECT, not idle-fleet failure.

## 2. Analysis streamlining (measured)
- 77 cycles / $273 / 21.6 agent-hours in 24h. Split: partial-refill n=48
  $187/13.3h (68% of spend), triage n=19 $52/6.4h, kick n=8 $27, meta $6.
- 18/48 refills ended "IDLE: nothing runnable" against a byte-identical
  board; median refill gap 26 min despite the exponential grace, because any
  zero-GPU doc/tool work touches CYCLE_WORKED and resets the backoff. Each
  IDLE refill re-derived the whole board (100-220 turns, ~$1.5-2.5).
- One triage cycle (20260909T135637) sat 2.4h/$11 waiting in-cycle on 2h
  on-pod panel evals; the 17:12 cycle did the same job correctly (launch
  on-pod, exit, evalready resume). Rare (1/77), no code change.

## 3. Changes MADE tonight (files, deltas, rationale)
- `watch_loop.py` +58: (a) board_fingerprint() gate — after a refill/kick
  declares IDLE, the watcher SKIPS further refills until ledger/backlog/
  code-HEAD/state-HEAD changes (completions, verdicts, snapshots re-arm;
  idle kicks keep their 4h-capped heartbeat; operator/MCP kicks ungated).
  Kills the 18-IDLE-refill class (~$40+/day). (b) refill prompt now points
  at `ops.sh board`. NEEDS WATCHER RESTART (action list below).
- `ops.sh` +45: new `board` subcommand — one-shot local digest (backlog,
  RUNNING/INTENT, unverdicted, newest journal heading + age per track) so
  surviving refills stop re-reading 7 STATUS docs.
- `snapshot.sh` +8: refuse empty/flag-looking run names (a real snapshot
  landed tagged "before --help" on 09-10 07:01).
- `test_watch_loop_refill.py` +44: 2 mechanics tests (fingerprint stability/
  re-arm; reap_cycles arms the gate only for IDLE refill/kick labels);
  36/36 pass in 0.11s.
- `rl_docs/tracks/walkcurr/STATUS.md` 6046 -> 6046 lines (same): stale
  WAITING-ON ("only live work is the 09-05 pilot" — closed today) replaced
  with the live items: Robot Lab bounded trial + design-note-first reopening.
- Standing docs untouched: ORCHESTRATOR_PROMPT/RESEARCH_RULES/RL_PLAN/
  CURRENT_TRUTHS/top STATUS.md all at their prior line counts.

## 4. Recommended, NOT made (and why)
- Downgrading refill cycles to a cheaper tier / turn cap: wait one day to
  measure the fingerprint gate first; may already cut refill spend 50%+.
- Any new walkcurr mechanism launch: forbidden by its own 8/8 closure —
  reopening requires a written design note argued from the closure evidence.

## 5. Open questions for the operator
- Both sim demos are delivered; physical trials are the remaining parent-goal
  gaps. Schedule Robot Lab for bundle_rlonly_v1 (mind the slew-derate note)
  and todaypolicy (resolve the model/regen mismatch first)?
- Accept the rl_only champion's known reverse/off-axis softness for a first
  bounded trial, or fund a dedicated deep-model design session on the
  front-pair sacrifice (all cheap levers exhausted; next idea must be new)?
