# META 2026-09-11 (nightly meta-analysis)

Action list — nothing needs an operator hand: watch_loop.py changed; its own
stale-code check self-triggers `restart_watcher.sh` (same path as the 09-10
board_fingerprint change). If it hasn't adopted by morning: `bash
rl_move/orchestrator/restart_watcher.sh`.

## 1. Progress toward the goals (last ~24h) — a genuinely big day
- **any_means (todaypolicy/standwalk/amp): op_20260910_50hz executed almost
  end-to-end.** 50 Hz exports now live for walk-role (GATE PASS + joygate),
  turn x2 seeds, teach-scripted-allhead walk (slip 2.57-3.08 vs 2.9 cap); the
  50 Hz bundle lacks ONLY stand/lower (RL_LOG 09-10 21:05..23:31). AMP closed
  honestly: styleoff control WALKS BETTER than the disc-alive sibling —
  style-credit for acquisition REFUTED, decisive (09-10 23:14-23:16).
- **rl_only (walkcurr): 3/3 seeds of 50 Hz warm-adapt walk PASS vs matched
  100 Hz parents (pooled gv 22-23/24), exports cut, bundle_rlonly_v2 supersedes
  the undeployable 100 Hz v1** (09-10 21:53..09-11 00:18). decleg architecture
  and RND scoping (mechanism classes 10-12) refuted cleanly. Off-axis/reverse
  remains THE open rl_only sim gap; the board correctly refuses filler on it.
- **standwalk: the stand50hz rise saga (~30 runs) CONVERGED.** Root-cause
  chain: dq-clamp misconfig -> late oversized PPO updates (clipsched) ->
  warm-start-inherited frozen log_std -> footprint-fade gradient hole ->
  stall-fight underpriced (2.64A rail; 0.41/tick charge vs 3.4/tick income).
  Winner `curhot-b23k12` (2M): 0/12 over_current, 12/12 valid_plant, fp
  30.75mm; +6M continuation OVERFITS — 2M checkpoint adopted. Next: stage-1
  mixed-start stance panel on b23k12, then 50 Hz stand/lower export ->
  bundle complete. Critique: early serial single-lever guessing; the dig-in
  discipline from 00:41 onward is the model to keep.
- **speed: honest ceiling found** — ps2.0 stride geometry is the optimum
  (0.0514 m/s peak, falls past 2.0); trained acq matches the ZERO-TRAINING
  teacher ceiling and stays command-invariant. Informative, but see open Q.
- Nothing consumed pods without purpose; backlog empty + 16/16 free at meta
  time is correct (post-convergence lull, named next levers in STATUS docs).

## 2/3. Bottlenecks measured (82 cycles, $381.46, 1850 agent-min in 24h)
- **Verdict races: 12 collision mentions in RL_LOG**, incl. a duplicated deep
  dig-in ($6.20/22m rebuilding the twin's rent design) and 2 FORCE-corrected
  contradictory verdicts. ~$25-40 + correction churn. FIXED (claims + dedupe).
- **Wrong-instrument misreads:** mixed-start `_gate` prestage standing in for
  the registered flat-pinned probe caused 2 wrong verdicts + 1 mispremised
  launch; cycles hand-ran that probe ~20x. FIXED (probe_args prestage).
- **Long serial cycles:** 174.8m/$18, 110.8m, 94.5m — dominated by 3-10 min
  synchronous on-pod eval waits (token cost ~0, but the delayed verdicts are
  what CREATE the races above). Claims mitigate; evalpending stays preferred.
- **pending_evals.json: 55/57 entries expired** (09-05..09-07), re-logged
  "expired; ignoring" EVERY watcher poll, never pruned. FIXED.
- Idle-churn check (yesterday's fix): board_fingerprint gating works — 5 idle
  refills ~$15 on 09-10 afternoon vs $46/18-cycle measured at the 09-10 meta.

## Changes MADE (code only; no standing doc grew)
- `watch_loop.py` (+58): (a) `claimed_runs()` + subtract from `newly` — a live
  `ops.sh review` claim now blocks duplicate triage-cycle spawns; (b) DIG-IN
  escalation deduped (one deep spawn per run per 4h); (c) expired pending
  evals pruned from the file once, not re-logged forever. Tests: 48 watcher
  tests pass; claim/claimed_runs round-trip exercised live.
- `ops.sh` (+54, review): auto-claims an unverdicted run for the calling
  cycle (flock claims.json, liveness-checked) and prints a CLAIMED banner for
  foreign live claims. Verified on a verdicted run (no claim) + live claim.
- `pod_eval.py` (+15): ledger field `probe_args` -> third DR-0 prestage pass
  (`<run>_probe`), args appended last so they override the standard shape.
- `rl_docs/COMMANDS.md` 617->628 (+11): gotcha #18 documenting probe_args
  (helper doc, not on the protected list; protected docs untouched).

## Recommended, NOT made (and why)
- Forbid refill cycles from triaging finished runs entirely (watcher owns
  them): prompt-surgery with real downside when spawn caps bite; claims
  should kill most races — re-measure at next meta before cutting.
- `full_mesh` (3.494kg) vs `mesh_mjx_twin` (4.806kg) mass-audit bug: real,
  open, unowned (COMMANDS #17) — needs a dedicated cycle, not meta-night code.
- One evalready cycle burned $7.81 in 12 turns/1.1m (prompt-size or cache
  anomaly) — single occurrence, measure again before touching spawn plumbing.

## Open questions for the operator
- **speed track scope:** trained speed matches the zero-training teacher
  geometry ceiling (~0.05 m/s) and stays command-invariant — is >0.05 m/s
  physical speed a goal worth a structural lever (gait-frequency/period DR),
  or does speed close as "geometry-limited, informative"?
- **amp track:** with style-credit refuted on the from-scratch 50 Hz recipe,
  does the AMP method close (milestones M5+ moot for acquisition), reserving
  it for style polish only?
