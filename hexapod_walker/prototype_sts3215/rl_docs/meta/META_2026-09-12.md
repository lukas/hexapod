# META 2026-09-12 (nightly meta-analysis)

Action list — nothing needs an operator hand or a watcher restart (ops.sh
changes take effect on next invocation).

## 1. Progress toward the goals (last ~24h)
- **speed (any_means): 0.08 m/s milestone MET.** Operator-ordered actuator-
  envelope ladder: 50 deg/s rung dominates (65 adds +1.4% for worse slip/roll);
  ss14 stacks cleanly — det 0.081 @0.10 pin, 0 falls/88 eps (RL_LOG 09-11
  14:39..18:27). bundle_speed_envelope_v1 GO_NOGO + drivevideo written. Track
  honestly closed at its frontier; no filler continuations funded.
- **amp/todaypolicy (any_means): turn-in-place freeze SOLVED by composition,
  not RL.** 6 RL lever classes (price/dose/budget/ramp/charge/RND, ~12
  canaries) refuted cleanly; probe_turn_compose (zero training) fixes gait
  sacrifice 4-seed clean, 3.4-3.9x |wz|; blend window + eval/drivevideo wiring
  landed; composed cap29-acq1 human_turn demo is visibly turning (wz_err 0.075,
  gait_valid). Composed full gate panel pending — next cycle reads it. This is
  exactly the any_means mandate working as designed.
- **standwalk: mostly burned the day.** ~20 canaries / 8 serial waves / ~9h on
  the dualbc7-anchor14coef1 walk-freeze. The first 3 waves of reward-pricing
  brackets were CONFOUNDED (bc_anchor_walk_coef=1 was itself freezing walk;
  isolated only at wave 6, 05:4x); worse, the 00:15 probe's code-read wrongly
  EXONERATED the anchor lever that a 30-min canary would have caught in wave 1.
  Queue is now correctly aimed (pricing retests on the anchor-0 base, 2 arms in
  flight). Mass-audit-bug (4.806->3.49 kg twin) found+fixed+fleet-re-verified
  with zero re-ships — the day's biggest validity win, near-zero GPU.
- **walkcurr (rl_only): honest dormancy.** bundle_rlonly_v2 stands post
  mass-fix (improves, 22/24 gv). Off-axis heading gap: 12 mechanism classes
  exhausted, needs a genuinely new structural idea — correctly not filler-fed.
- Both parent goals' physical legs sit with Robot Lab; cloud queue shape
  (candidates + evidence + composed packaging) is right.

## 2/3. Bottlenecks measured (85 cycles, $316.02, 1426 agent-min, 78 sonnet/7 fable)
- **Eval-babysitting outlier: one refill cycle $35.65 / 519 turns / 41.9m**
  (09-11 17:03) — 467 kubectl execs + 30-min podwaitlog waits on two CPU evals,
  when `evalpending add` + exit existed (used correctly at 08:07 same day).
  11% of the day's spend in one cycle. FIXED (podwaitlog cap, below).
- **Stale board digest:** `ops.sh board` (built 09-10 to stop STATUS re-reads)
  only matched `^## ` headings; hot tracks write "Update, <date>" paragraphs,
  so standwalk showed a 09-05 "Next", amp "WAITING-ON", joystick "Goal" —
  refills therefore still hand-tailed STATUS docs 20-40x/cycle (measured on
  09-12 logs: 21/26/39 reads). FIXED (board now shows real newest entry).
- **Serial dose-arm dribbling (standwalk):** 8 waves of 1-3 arms over 9h with
  15 slots free, against the operator's 08-22 batch directive; plus the
  confound above forced ~7 arms to be re-run. GPU cost trivial; real cost
  ~15 serialized triage/refill cycles (~$40) + ~6h wall clock on the answer.
  Discipline miss, not tooling — flagged for the report only (see below).
- Idle-churn: 11 IDLE-ending cycles $18.77 (healthy vs $46/18-cycle on 09-10);
  claims fix holding — zero verdict races; one duplicate-config LAUNCH (02:02,
  differing name/flag spelling defeats the twin-guard normalizer) was caught
  and killed by the agent itself, cost one canary slot-hour.

## Changes MADE (code only; no standing doc touched)
- `ops.sh` board `head()` (+15 net): match `## `/`Update, `/`Last updated:`
  conventions, print the newest entry's first 4 lines per track — makes the
  refill survey a 1-command read. Smoke: live board shows all 7 tracks' real
  09-11/09-12 entries.
- `ops.sh` podwaitlog (+9): default timeout 1800->600s, hard cap 600s unless
  WAIT_LONG=1, with an explicit `evalpending add` + exit pointer. Smoke: cap
  message verified live; bash -n clean. (ops.sh 1765->1788 lines, code file.)

## Recommended, NOT made (and why)
- **standwalk stop-loss:** if the in-flight anchor-0 pricing pair fails, the
  next wave should be ONE pre-registered batch of every remaining named lever;
  if that fails too, park the dualbc7 RL-fine-tune lineage and route
  unification via todaypolicy's composed delivery (its charter allows it)
  while stage-2 is redesigned. Not written into STATUS mid-flight (two arms
  running; their triage owns the fork) — logline carries it.
- Semantic (post-normalization) config dedupe in the launcher: 1 occurrence/
  day, agent-caught, launcher complexity not yet justified.
- "Cheap empirical beats code-read exoneration" as a rule: judgment lesson,
  anti-bloat says don't grow RESEARCH_RULES for one instance.

## Open questions for the operator
- 09-11's speed question self-resolved (0.08 m/s met via envelope ladder).
- standwalk unification: given composed delivery is working, what budget cap
  should the dualbc7 RL-fine-tune lineage get before "composed is the ship"?
