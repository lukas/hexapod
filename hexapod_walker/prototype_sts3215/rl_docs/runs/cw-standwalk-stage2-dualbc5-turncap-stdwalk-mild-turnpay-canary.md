# cw-standwalk-stage2-dualbc5-turncap-stdwalk-mild-turnpay-canary

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-31T07:40:26+00:00

**pod**: hexapod-mjx-train-1

**steps**: 2000000

**parent**: cw-standwalk-stage2-dualbc5-turncap-turnpay-canary

**wandb_id**: 0tcfig0w

**hypothesis**: Plain English: the last 5 turn-authority mechanism classes (anchor dose 3.0/1.0/0.3, anchor turn-tick-targeted skip, BC-anchor isolate-update, PPO ent-coef 4x) all left wz_med under the 0.03 FAIL floor -- the STATUS dig-in found the entropy-boost arm was a weaker test than it looked (train/std never left ~0.223 the whole 2M run despite entropy_loss rising 20x), so PPO's own gradient may simply never widen the walk core's action noise enough on its own to discover the turning behavior even with more exploration REWARD pressure. This tests a guaranteed-to-move DIRECT lever instead: using the new multi-core log-std-anneal support (this cycle's own code, plus an argv-parsing fix after the first launch attempt of this exact arm crashed pre-boot on a comma-list-vs-argparse-negative-number gotcha -- see _fixup_log_std_final_argv), forcibly raise the walk core's log_std from its current ~-1.5 (std 0.22) to -0.8 (std ~0.45) over the first 10pct of steps and PIN it there for the rest of the run (PPO cannot fight it back down), while leaving the already-proven stance-core cooling (-4.0 over 50pct) untouched. Prediction-if-true: probe_turn_authority wz_med clears 0.03 both signs without gait collapse. Prediction-if-false: identical frozen-body read despite genuinely wider action noise (env/train/std should confirm >=0.4 for real, unlike the entboost arm) -- exploration MAGNITUDE is refuted too, next suspect is a structural credit-assignment defect in the shared value function / GAE over the minority turn-tick states, requiring the raw per-tick reward-vs-value trace tool named in the prior STATUS update.

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. probe_turn_authority (own cfg, wz_cmd=+-0.25, walk-mode-filtered): PASS if wz_med clears 0.03 in the commanded direction both signs; joint with the -hi sibling. Also check env/train/std actually reached >=0.35 by 200k steps (confirms the lever moved, distinguishing 'exploration didn't help' from 'exploration didn't happen' the way the entboost arm's flat std did) and walk_det frame-strip for gait collapse (6-leg cycling preserved is required for any PASS read, a collapsed gait 'turning' via falling over does not count).

**verdict**: CANARY FAIL - MECHANISM (joint with -hi sibling, 2-point dose-response, 6th+7th turn-authority mechanism classes refuted -- exploration MAGNITUDE now exhausted). Plain English: forcibly widening the walk-core action noise (log_std -1.5->-0.8, std 0.22->0.45, pinned by 200k) still produced ZERO turn-in-place authority at 2M steps. probe_turn_authority (own 96-key cfg, wz_cmd=+-0.25, walk-mode-filtered, seeds 0/1, controller-side rollout on the synced checkpoint): wz_med +0.0005/-0.0008 (wz_cmd=+0.25) and -0.0061/-0.0039 (wz_cmd=-0.25) -- all four readings under the gates own 0.03 FAIL floor both signs, wz_p90_abs only 0.038-0.057 (noise-scale, not a turn), med|wz_err|=0.248 vs frozen-body prediction 0.25. Lever CONFIRMED to have actually moved (unlike entboosts flat std): env/train/std read 0.223 at step 131k, crossed 0.35 by ~262k, pinned at 0.449 for the back 90% of the run -- this is a real, substantial widen, not a no-op. env/walk_yaw_kernel_factor erodes 0.45->0.06, identical shape/magnitude to every prior canary in this lineage (turnpay, anchor doses, turnskip, isolateoff, entboost); env/walk_wz stays pinned within +-0.01 the whole run despite the wider noise -- the extra exploration never even sampled its way into more wz variance during training, let alone learned to use it. Reward crashes hard through the back half (quarters [54.7,47.4,-221.1,-13.4], same shape as every sibling canary) -- not a rising-reward case per the 08-21 ruling. Own frame-strip check (walk_det_0.png, controller-pulled from train-1): clean straight-walk gait preserved through the full 30s episode -- feet pattern goes plant(######)->tripod-alternating(.#.#.#), v=0.056 tracking ref=0.08 m/s by t=30s, tilt small, no fall/collapse -- rules out "more noise traded for gait collapse", the disjunctive FAIL clause is met cleanly on the wz_med floor alone. Conclusion: exploration MAGNITUDE at the PPO action-noise level is now REFUTED as the turn-authority bottleneck (joint with -hi below), matching the runs own pre-registered exit clause. This is the 6th distinct turn-authority mechanism class refuted on this lineage (after anchor dose x3, anchor targeted-skip, BC-anchor isolate-update, PPO ent-coef, and now direct log-std dose x2) -- no further "exploration lever" arms are funded without a new idea. Next suspect (named by this runs own gate/hypothesis text): a structural credit-assignment defect in the shared value function / GAE over the minority turn-tick states -- requires building the raw per-tick reward-vs-value trace tool before another training-mechanism knob. NOT HARDWARE-READY. Evidence: logs/ckpt_eval/turn_probe_stdwalk_mild.json, wandb 0tcfig0w.

