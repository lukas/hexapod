# cw-standwalk-stage2-dualbc6-turncap-mirroraug-stillbal-acq1-s1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: PARTIAL

**created**: 2026-08-31T19:58:57+00:00

**pod**: hexapod-mjx-train-1

**steps**: 38000000

**parent**: cw-standwalk-stage2-dualbc6-turncap-mirroraug-yaw5x-acq1

**wandb_id**: qdhjgs0j

**hypothesis**: Plain English: seed-1 twin of stillbal-acq1 — does symmetric rotation pricing (k_yaw_still 50->5, the only change vs the refuted yaw5x-acq1 recipe) stop PPO from trading turn authority away? Campaign convention: every retention arm runs as a 2-seed pair; yaw5x showed real seed variance in erosion DEPTH (seed0 froze at 0.024-0.039, seed1 held a 0.045-0.116 mid-band), so a single seed cannot separate 'slowed erosion' from seed luck. Same trace evidence as the seed0 twin (probe_yaw_credit income accounting: turning is net-profitable +1.0-1.3/tick yet erodes; critic value degenerate-constant; the 10x still-vs-turn pricing asymmetry on 50% of commands is the last unpriced suspect). Prediction-if-true: snapshot curve holds materially above yaw5x-acq1-s1's measured mid-band (0.045-0.116) and final reads >=0.10 both signs. Prediction-if-false: same erosion band as its 1x/5x predecessors.

**gate**: ACQUISITION retention test, seed-1 twin — SAME gate as cw-standwalk-stage2-dualbc6-turncap-mirroraug-stillbal-acq1: PASS if final probe_turn_authority wz_med >= 0.10 both signs AND gait_valid >= 5/6 zero falls AND pure-walk det progress_ratio in/above 0.40-0.48. FAIL if snapshot curve matches the yaw5x pair's measured erosion. DRIFT-BREAK FAIL if zero-cmd heading drift returns beyond noise. PARTIAL otherwise — joint read with seed0.

**verdict**: ACQ FAIL - PRICING-SYMMETRY RETENTION ALSO REFUTED, JOINT CLOSE 2/2 (own probe). Result: seed-1 twin of stillbal-acq1 (k_yaw_still 50->5, 10x less anti-rotation tax on zero-yaw-cmd segments) erodes the same way. Built the full 8-point probe_turn_authority curve myself (7 --snapshot-every=5M snapshots + final, on train-1, own TURNCAP_CFG_SET): 5M pos +0.076/+0.077, neg -0.129/-0.130 (close to seed0's +0.075/+0.074 / -0.105/-0.115); decays to final 38M pos +0.046/+0.069, neg -0.073/-0.078 (seed0 final: +0.046/+0.053 / -0.047/-0.049) -- both signs decisively under the >=0.10-both-signs PASS bar, matching the yaw5x pair's measured erosion shape almost exactly (fast-early-then-floor). No DRIFT-BREAK: env/walk_direction_err_deg stayed in a 60-94deg band the whole run (wandb_history.csv, never blew out) -- cutting k_yaw_still did not resurrect heading-hold drift. No gait-collapse confound: eval/walk/survived_frac=1.0 throughout, own walk_det_5 frame strip (in-flight harness, train-1) shows clean six-leg gait, upright, no fall. Reward rose then plateaued in the last two quarters (2818->2912->3232) -- RL-erosion signature, not undertrained. Why: this closes the JOINT verdict with seed0 (already ACQ FAIL, same read) -- BOTH leverable reward-side/data-side fixes for turn-authority erosion (5x turn-pay dose, 10x anti-rotation-tax cut, 2x turn-segment density) are now refuted on both seeds. Per the gate's own escalation path and probe_yaw_credit's mechanistic finding (critic V degenerate-constant across command signs), the next lever is architecture-side: an explicit critic-side feature or a dedicated value-warmup phase. What's next: found this ALREADY BUILT (not new-code as assumed) -- train_ppo_mjx.py's --actor-lr/--actor-freeze-steps/--critic-lr (update_health.py, built 08-18 for a different purpose, 23/23 tests green) is exactly a value-warmup mechanism (freeze actor mean+std, let critic keep learning on the frozen policy's own rollouts). Found and fixed a real gap before trusting it: the critic's own dedicated recurrent trunk (lstm_critic, present on every --gru/--gru-dual/--gru-experts policy) shares no substring with the stock critic markers (value_net, vf_features_extractor) -- an unextended split would freeze lstm_critic right alongside the actor, defeating the warmup (0 prior --actor-lr + --gru* combos in experiments.json -- never caught before). Fixed in train_ppo_mjx.py (extend critic_markers with lstm_critic whenever any --gru* flag is active), added 2 tests (test_gru_dual_default_split_misclassifies_lstm_critic_as_actor documents the gap, test_gru_dual_lstm_critic_rides_critic_lr_with_marker_extension pins the fix), 25/25 test_value_learning.py green, snapshot exp/standwalk-valuewarmup-lstmcritic-marker-fix. Launching the value-warmup acquisition pair off the same turnpay-canary RETENTION base this cycle.

