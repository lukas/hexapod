# cw-standwalk-stage2-dualbc6-turncap-mirroraug-turnpay-acq1-s1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: PARTIAL

**created**: 2026-08-31T15:49:29+00:00

**pod**: hexapod-mjx-train-3

**steps**: 38000000

**parent**: cw-standwalk-stage2-dualbc6-turncap-mirroraug-turnpay-canary-s1

**wandb_id**: r4wypksg

**hypothesis**: Plain English: seed-1 twin of cw-standwalk-stage2-dualbc6-turncap-mirroraug-turnpay-acq1 -- same question (does the mirror-augmented turn-authority base keep its turn authority and mature walk quality under a full acquisition budget, or does a slow erosion invisible at 2M show up over a longer horizon), replicated on the second seed whose own 2M RETENTION canary read wz_med +0.129/+0.123 and -0.146/-0.160 (essentially unchanged from its own pre-RL base +0.148/+0.148,-0.152/-0.158, i.e. near-zero erosion) with det walk gait_valid 24/24 (own gate pass, this cycle) and zero terminations. Prediction-if-true: at ~40M cumulative steps this seed also holds wz_med>=0.10 both signs, progress_ratio holds/improves, gait_valid>=5/6, and eval_joystick_gate direction-following materially improves vs the ~45-52deg non-turning-base baseline. Prediction-if-false: wz_med decays toward the <0.03 floor over the longer horizon on this seed too. Running both seeds as a matched pair (not a solo bet) lets a single-seed anomaly be told apart from a real class-level property of the mirror-augmented base.

**gate**: ACQUISITION (continuation of a passed RETENTION canary, phase acquisition, budget ~40M cumulative), seed-1 twin of the -acq1 arm -- SAME gate: PASS/promote-to-stage2-source if probe_turn_authority wz_med >= 0.10 both signs at the final checkpoint AND pure-walk det progress_ratio not regressed vs 0.40-0.48 AND det walk gait_valid >= 5/6 AND eval_joystick_gate direction_err_med materially improved vs the 45-52deg non-turning-base baseline with zero-or-near-zero falls and slip/m <=3.0. FAIL if wz_med decays back under 0.03 both signs by the final checkpoint despite clean gait/progress. PARTIAL/DIG-IN otherwise; read jointly with the -acq1 (seed0) twin the way the source canaries were.

**verdict**: PARTIAL - EROSION (own scope; joint fork with -acq1/seed0 read separately by a concurrent cycle). Result: the decisive gate clause (probe_turn_authority, own TURNCAP_CFG_SET, wz_cmd=+-0.25, seeds 0/1, walk-mode-filtered) on this seed's final ~40M-cumulative checkpoint reads wz_med +0.023/+0.029 (wz_cmd=+0.25) and -0.078/-0.070 (wz_cmd=-0.25) -- rules out PASS (needs >=0.10 both signs) but is NOT the clean 'both signs <0.03' FAIL either: the positive sign sits right at/under the 0.03 floor (matches every non-mirrored mechanism-class collapse) while the negative sign retains real, above-floor authority at ~2.3-2.6x the fail threshold (down from this seed's own pre-acquisition RETENTION-canary read of +0.130/+0.121 / -0.178/-0.172 -- roughly 75-80% erosion on the + sign, 55-60% on the - sign). This is exactly the gate's own pre-registered PARTIAL/DIG-IN case ('one sign erodes and the other does not... do not force a binary call'). Evidence: logs/ckpt_eval/probe_turn_authority_dualbc6_turncap_mirroraug_turnpay_acq1_s1.json (my own read, cross-checked against the concurrent cycle's near-identical independent read in STATUS ~17:2x: +0.025/+0.029, -0.072/-0.057). New root-cause evidence this cycle: read env/reward_walk_yaw, env/walk_yaw_kernel_factor and env/yaw_prog_wz_avg from this run's own full wandb_history.csv in 20 even bins across the WHOLE 38M-step continuation -- env/yaw_prog_wz_avg sits in a flat +-0.004 noise band from the VERY FIRST bin (near step 0 of the continuation) through the last, never showing a healthy nonzero signal at any point, while reward_walk_yaw/walk_yaw_kernel_factor stay roughly flat (~0.21-0.35, mild noisy decline, no cliff). This is the FAST-EARLY-THEN-FLOOR shape the gate text named as the RL-erosion signature (matching all 8 prior non-mirrored mechanism-class canaries), not a 'still actively eroding at cycle end' shape -- i.e. most of the damage likely happened early in this 38M-step continuation and then plateaued, so a shorter acquisition budget from the same RETENTION checkpoint is the more promising lever than 'train longer/more budget' per the 08-21 ruling (reward is not misaligned-but-rising here in the yaw channel specifically; it is flat, and task-metric flat+adequate-budget is the ruling's own FAIL/erosion case, not a 'needs more steps' case). Why this matters: mirror-augmentation gave a real, above-baseline HEAD START (the RETENTION canary genuinely passed, first of 9 mechanism/base classes to do so) but does not survive full ~40M acquisition-budget RL pressure intact -- it is degraded, distinctly better than the naked mechanism classes' near-total both-sign collapse, but well short of durable. Gait_valid/pure-walk progress_ratio/eval_joystick_gate direction_err_med (the gate's other 3 clauses) are still mid-flight on-pod (train-3, ~30min in of a typically 1-2h video-eval class, ps-confirmed alive, not duplicated) -- those cannot flip this PARTIAL to PASS (clause 1 already forecloses that) but will decide whether walk quality also degraded or held. Next: a later cycle should (1) read gate/owncfg/joygate once synced to close out whether walking quality/direction-following also eroded, (2) given the fast-early-floor shape, consider a shorter-budget acquisition arm (e.g. 8-10M instead of 38M) off the SAME RETENTION checkpoint as the next real lever, testing whether stopping before the erosion floor preserves more of the canary's turn authority while still maturing walk quality -- distinct from and complementary to whatever the -acq1 (seed0) sibling's own close decides.

