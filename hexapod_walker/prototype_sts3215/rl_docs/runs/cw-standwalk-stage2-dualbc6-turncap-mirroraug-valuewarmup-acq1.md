# cw-standwalk-stage2-dualbc6-turncap-mirroraug-valuewarmup-acq1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: ACQ_FAIL

**created**: 2026-08-31T22:45:47+00:00

**pod**: hexapod-mjx-train-0

**steps**: 38000000

**parent**: cw-standwalk-stage2-dualbc6-turncap-mirroraug-turnpay-canary

**wandb_id**: n5wv7ny7

**hypothesis**: Plain English: does giving the critic a head start (actor frozen, critic still learning) before PPO resumes fix the credit-assignment blindness the turn-authority campaign found, rather than the actor immediately drifting the behavior while the critic is still catching up? probe_yaw_credit found the critic's forward value_delta is BLIND/PUNISHES on this exact turnpay-canary checkpoint at 2M already (15/16 probes across this whole lineage) and V is near-degenerate-constant across command signs -- but every acquisition arm so far co-trains actor+critic from step 0, so the critic never gets a stationary target to learn against before the actor (which already has real turn authority from mirror-aug BC) starts moving under a value function that cannot yet defend it. This is the SAME base/recipe as the already-FAILED turnpay-acq1 (ACQ FAIL - TURN EROSION, wz_med decayed to +0.024/+0.034,-0.069/-0.073) with exactly ONE new variable: --actor-lr=0.0003 (== the run's own constant --lr, so unfrozen behavior is identical) + --actor-freeze-steps=8000000 (~21% of the 38M budget) so the critic alone updates on the canary's frozen, already-turning policy for the first 8M steps before any actor drift can begin -- a genuine value-warmup phase, not the density/pricing-magnitude levers already refuted (turndense1, stillbal, yaw5x). Also fixed a real gap this required: train_ppo_mjx.py's critic-marker split previously misclassified the dual-core critic's own recurrent trunk (lstm_critic) as ACTOR, which would have frozen the critic's encoder too and defeated the whole point -- fixed + tested this cycle (test_value_learning.py, 25/25 green), 0 prior --actor-lr+--gru* combos existed to have caught it. Prediction-if-true: probe_yaw_credit run on the 8M snapshot (still-frozen boundary) flips from CREDIT-BLIND/PUNISHES to CREDIT-REWARDS (corr>=+0.15) on most of the 4 wz_cmd x seed-probe combos, AND probe_turn_authority wz_med at that same snapshot still reads near the canary's own +0.13/-0.18 (unchanged, since the actor never moved) -- confirming the freeze mechanically worked and giving the critic a real chance. Prediction-if-false: probe_yaw_credit stays BLIND/PUNISHES even after 8M critic-only steps against a STATIONARY policy -- ruling OUT co-training non-stationarity as the cause and pointing at a genuine representational/capacity limit (the wz-reward component's variance is too small relative to the dominant terms for MSE-return regression to resolve it) as the next, harder architecture fix (multi-head/reward-decomposed critic).

**gate**: Read at TWO checkpoints via probe_yaw_credit (own 96-key TURNCAP_CFG_SET, wz_cmd=+-0.25, seeds 0/1) + probe_turn_authority: (1) the ~8M freeze-boundary snapshot -- MECHANISM check: PASS-mechanism if >=3/4 probe_yaw_credit combos flip to CREDIT-REWARDS (corr>=+0.15) AND probe_turn_authority wz_med stays >=0.10 both signs (actor unmoved, so this should hold trivially -- a miss here means the freeze itself broke something, e.g. a BN/dropout-style train/eval mismatch, and voids the read); FAIL-mechanism (credit stays BLIND/PUNISHES on >=3/4 combos) means non-stationarity is refuted as the cause, stop here, do not read the final checkpoint as a turn-authority result (the mechanism already failed). (2) IF mechanism passes: read the final 38M checkpoint the same way -- PASS if final probe_turn_authority wz_med >= 0.10 both signs AND gait_valid >= 5/6 zero falls AND pure-walk det progress_ratio in/above 0.40-0.48 (durable fix). PARTIAL if credit flips but turn authority still erodes post-unfreeze (warmup fixes the credit signal but something else still drives erosion -- e.g. the actor's own PPO-clipped policy gradient still drifts turning away even with a now-informative advantage, motivating an explicit reward weight/entropy lever ON TOP of the warmup). FAIL if authority erodes to the same <0.05 floor as turnpay-acq1 despite a fixed credit signal.

**verdict**: ACQ FAIL - MECHANISM PASS, DURABILITY FAIL; JOINT CLOSE 2/2 (with -s1): value-warmup genuinely fixes critic credit assignment but does NOT defend turn authority, and the dig-in found the concrete non-credit driver. Ran the gate's own two-checkpoint read + a 12-point erosion curve myself on-pod (train-0, own TURNCAP_CFG_SET). (1) 8M freeze-boundary (s8126464): probe_yaw_credit forward_verdict 3/4 CREDIT-REWARDS (corr +0.31/+0.26/+0.24; 4th combo corr +0.23 classified BLIND) -- MEETS the gate's >=3/4 PASS-mechanism bar, the first full mechanism-pass of the entire campaign (s1 got 2/4 pos-only; prior baseline 15/16 BLIND/PUNISHES): co-training non-stationarity WAS a real cause of the critic's credit blindness. probe_turn_authority +0.108/+0.114,-0.147/-0.152 -- >=0.10 both signs, freeze held authority. (2) Final 38M: authority +0.029/+0.032 (pos, AT the turnpay-acq1 floor), -0.069/-0.065 (neg) -- decisively under the 0.10 PASS bar; and probe_yaw_credit is 4/4 BLIND again ALREADY BY 16M (corr 0.09-0.14; final 0.14-0.18) -- the credit fix itself DECAYS once co-training resumes. Curve: 2M/8M ~canary (+0.116/+0.108), 10M pos 0.063 (-43% within 2M of unfreeze), 12M rebound 0.088, then chronic grind 14M 0.072 / 16M 0.060 / 20M 0.040 -> floor. ROOT CAUSE (dig-in, wandb_history both seeds): the FIRST post-unfreeze update realized approx_kl 3.76 (s1: 4.90) vs 5e-6 frozen, staying 0.2-0.8 for ~1.5M steps -- an unfreeze advantage shock (8M of critic-only convergence -> large confident advantages -> actor slams in at full LR), and the collapse window coincides exactly with it. Beyond the shock, EVERY run in this family trains at approx_kl 0.08-0.15 / clip_fraction ~0.28 steady-state DESPITE default --target-kl 0.02 (SB3 early-stop cannot undo an applied minibatch -- update_health.py's own documented gap), which both grinds the thin yaw behavior away AND re-blinds the critic via nonstationarity. So critic blindness is real but NOT sufficient: oversized realized policy updates are the second driver, now measured. METHODOLOGY NOTE: env/reward_walk_yaw declined 0.37->0.24 during the freeze with the actor PROVABLY frozen -- that in-training decline shape is an env/logging artifact, not erosion evidence; do not cite it as such again. Evidence: logs/ckpt_eval/{yaw_credit,turn_authority}_dualbc6_turncap_mirroraug_valuewarmup_acq1_{2M,8M,10M..36M,16M,final}.json (built this cycle on train-0). Standard gate/owncfg/mixedsession mid-flight on train-0 at verdict time -- informational, cannot flip (authority clause decisively missed; in-training eval/walk/survived_frac 1/1 late-run, no gait-collapse signal). NEXT (launching this cycle): the exact countermeasure already exists built+tested but was never used with this family -- --kl-rollback (attach_kl_rollback: transactional update, rollback + actor-LR backoff on realized KL over ceiling). Pair: valuewarmup+klroll (does bounded KL preserve the warmup's credit fix AND authority?) and klroll-only no-freeze control (is bounded KL alone sufficient?), 0.05 ceiling, one seed each under the 80M/cycle cap; seed twins next cycle if informative.

