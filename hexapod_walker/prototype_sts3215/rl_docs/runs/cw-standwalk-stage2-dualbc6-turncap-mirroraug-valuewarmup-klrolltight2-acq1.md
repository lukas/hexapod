# cw-standwalk-stage2-dualbc6-turncap-mirroraug-valuewarmup-klrolltight2-acq1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: ACQ PARTIAL - GUARD-REJECT-ALL (CRITIC-ONLY ARTIFACT), AXIS CLOSED

**created**: 2026-09-01T06:16:01+00:00

**pod**: hexapod-mjx-train-0

**steps**: 38000000

**parent**: cw-standwalk-stage2-dualbc6-turncap-mirroraug-valuewarmup-klrolltight-acq1

**wandb_id**: ocxe34st

**hypothesis**: Plain English: pushing the update-size cap even tighter (0.02->0.01, now BELOW the trainer's own --target-kl=0.02) on top of the already-validated freeze+guard combo -- does turn authority keep climbing toward the >=0.10-both-signs PASS bar, or does the policy stop being able to adapt at all? This cycle's own 12-point probe_turn_authority curve on valuewarmup-klrolltight-acq1 (freeze 8M + kl-rollback=0.02) verdicted PARTIAL-BETTER vs the 0.05 sibling: final pos 0.0803/0.0756 (up from 0.068/0.068), neg -0.1131/-0.1175 (more negative than -0.109/-0.110) -- both signs improved, confirms the update-size dose-response holds even combined with freeze, exactly the gate's own PARTIAL-BETTER branch which names 'one more notch down' as the next step. In-training clean throughout (eval/walk survived_frac=1, walk_startjitter survived_frac=1, reward rising every quarter, kl_rollback_count=8 total, realized approx_kl bounded 0.012-0.019 post-unfreeze -- tighter than the 0.02 arm's own 0.01-0.03 band, confirming the guard is engaging harder). Identical recipe to valuewarmup-klrolltight-acq1 (actor-freeze-steps=8000000, same turnpay-canary init, same seed 0) except --kl-rollback 0.02->0.01.

**gate**: PASS/promote-to-stage2-source if final (38M) probe_turn_authority (own TURNCAP_CFG_SET) wz_med >=0.10 both signs AND det walk gait_valid>=5/6 zero falls AND purewalk det progress_ratio in/above 0.40-0.48. PARTIAL-BETTER if final wz_med improves over valuewarmup-klrolltight-acq1's own final (pos>0.078, neg more negative than -0.115) but still misses PASS -- dose-response still monotonic, motivates yet one more notch (0.005) or a seed twin to confirm. PARTIAL-SAME/WORSE if final reads land within noise of or below the 0.02 sibling's plateau -- 0.02 already captured the achievable gain from this lever; stop tightening, escalate to reward-decomposed critic for the remaining positive-sign gap. FAIL if gait_valid or progress_ratio regress hard vs the 0.02 sibling (cap too tight, starves adaptation entirely) -- bracket back up toward 0.02/0.015.

**verdict**: Result: --kl-rollback=0.01 is unusable at this policy family's realized-KL scale -- this run is an accidental 38M-step CRITIC-ONLY continuation, not a genuine tighter-dose actor test. Evidence (built+pushed by the flagging cycle, re-read this cycle): kl_rollback_count=457 with the LATEST update itself rolled back (train/kl_rollback=1 at final); direct state_dict diff 2M-snapshot vs final shows every ACTOR tensor (mlp_extractor.policy_net/action_net/lstm_actor core_a+core_b/log_std_a) at EXACTLY 0.0 max-abs-diff while CRITIC tensors show real 0.03-0.57 diffs -- the actor never moved a single float after the 8M unfreeze, because 0.01 sits below this family's normal realized-approx_kl operating range (~0.08-0.15/raw update), so every post-unfreeze update got rejected-and-restored. Its nominal probe_turn_authority wz_med (pos avg 0.1116, neg avg -0.1654) nominally clears the >=0.10-both-signs bar but is a FALSE POSITIVE: it is just the untouched turnpay-canary-derived actor init's own pre-existing turn skill carried forward by a critic-only run, not RL-preserved authority -- never cite it as a PASS. Why (the fork): decided NOT to fund a guard scale-down-and-retry code fix, because the campaign already closed the whole update-size-constraint mechanism family this same window (klrolldriftmatch-acq1{,-s1} joint close, matched-drift confirmed n=4): even a REPAIRED guard doing genuine actor training under a tighter cap would, per that closure, converge to the same ~0.075-0.09 pos / ~-0.10-0.12 neg ceiling as accumulated actor-training-steps dominate regardless of freeze/cap mechanics -- fixing the guard would just re-spend budget re-discovering 0.02's own plateau, not open new ground. Verdict: STOP -- the kl-rollback dose axis closes at 0.02 (already-recorded clean band: valuewarmup-klrolltight-acq1 final pos 0.0803/0.0756, neg -0.1131/-0.1175); no relaunch, no guard-scale-down fix funded on this line. What's next: the campaign's only remaining lever is the reward-decomposed command-conditioned critic (STATUS Next item 2, scoped 09-01) -- genuine new architecture, not another dose. Evidence: logs/ckpt_eval/turn_authority_dualbc6_turncap_mirroraug_valuewarmup_klrolltight2_acq1_curve.json, logs/ckpt_eval/klrolltight2_acq1_weightdiff_2Mvfinal.json.

