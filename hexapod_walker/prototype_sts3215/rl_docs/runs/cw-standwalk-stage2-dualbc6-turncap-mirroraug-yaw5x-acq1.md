# cw-standwalk-stage2-dualbc6-turncap-mirroraug-yaw5x-acq1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: ACQ FAIL - PRICING RETENTION REFUTED

**created**: 2026-08-31T17:38:47+00:00

**pod**: hexapod-mjx-train-6

**steps**: 38000000

**parent**: cw-standwalk-stage2-dualbc6-turncap-mirroraug-turnpay-acq1

**wandb_id**: ht0e4dui

**hypothesis**: Plain English: can paying the robot 5x more for turning DEFEND the turn authority it already has, where normal pay let it slowly trade turning away for easier income? The mirror-augmented base holds real turn authority (wz_med +-0.13-0.18) through a 2M canary, but under the standard reward stack a full 38M acquisition eroded it 60-75% to the frozen-body band (acq1/acq1-s1, verdicted this cycle) while ep_rew kept rising -- PPO's optimum at 1x yaw pricing is near-zero turn authority. The earlier yawscale5x/15x REFUTATION does not cover this case: those arms started from an ALREADY-FROZEN non-mirrored base (a recovery problem -- pricing cannot make random joint noise rediscover a coordinated multi-joint turn), whereas this arm starts from the mirror canary checkpoint WITH live authority (a retention problem -- pricing only has to make defending an existing behavior worth more than abandoning it). Identical recipe to the failed acq1 (same init ppo_goal_..._mirroraug_turnpay_canary.zip, same levers) except k_walk_yaw/k_yaw_prog 1.0->5.0, plus the new --snapshot-every=5000000 (off-by-default tool, smoke-verified) writing step-tagged checkpoints so the erosion time-constant is finally measurable instead of inferred from 2 endpoints. Prediction-if-true: probe_turn_authority wz_med >= 0.10 both signs at the final ~40M checkpoint and the snapshot curve shows authority holding roughly flat. Prediction-if-false: same erosion curve as acq1 (authority halves by ~10-20M, frozen band by 40M) despite 5x income -- reward-magnitude retention is then also refuted and the next escalation is the per-tick reward-vs-value credit-assignment trace the yawscale canaries named. Strongest alternative: 5x yaw pricing destabilizes or distorts the base walk (overweighted small term) -- caught by the gait_valid and progress_ratio clauses.

**gate**: ACQUISITION retention test. PASS/promote-to-stage2-source if at the final ~40M checkpoint: probe_turn_authority (own TURNCAP_CFG_SET, wz_cmd=+-0.25) wz_med >= 0.10 both signs AND pure-walk (mode_seq=0) det progress_ratio in/above 0.40-0.48 AND det walk gait_valid >= 5/6, zero falls. FAIL (pricing cannot defend authority either) if the snapshot erosion curve matches acq1's shape and final wz_med lands under 0.05 either sign with gait clean. GAIT-BREAK FAIL if gait_valid < 5/6 or progress_ratio collapses (5x term distorts walking) -- then bracket down to 2-3x. PARTIAL otherwise: quantify erosion curve from the _s<steps> snapshots, do not force a binary call.

**verdict**: 5x yaw pricing does NOT defend turn authority any better than 1x did. Evidence: built a full 8-point probe_turn_authority (own TURNCAP_CFG_SET, wz_cmd=+-0.25) erosion curve from this run's own --snapshot-every=5M checkpoints (5M/10M/15M/20M/25M/30M/35M/final-38M, ran myself on-pod train-6, not part of the standard prestage) instead of the 2-endpoint reads every prior arm had. Result: authority collapses FAST -- by 5M steps into this continuation, pos wz_med is already 0.067-0.073 (down from the canary-init +0.13) and neg is -0.107 to -0.124 (down from -0.178), i.e. already 40-50% eroded before 5x pricing gets any real chance to act. By 15-20M pos sits at/under the 0.03-0.05 frozen-body floor (0.030-0.039) and stays there (noisy) through 38M; neg partially holds (-0.045 to -0.10, noisy, one late blip up at 35M that decays back by the final checkpoint). Final: pos +0.024/+0.034, neg -0.069/-0.073 -- decisively under the >=0.10-both-signs PASS bar, matching (if anything faster than) the REFUTED 1x turnpay-acq1 shape the gate named as its FAIL comparison. Cross-checked against the seed-1 twin's own independently-run snapshot curve (logs/ckpt_eval/probe_turn_authority_..._yaw5x_acq1_s1*.json, a concurrent cycle's own instrument, not touched/duplicated here): same qualitative shape, final pos +0.084/neg -0.070 -- also far under PASS, corroborating this is a class property of 5x pricing, not seed noise. Supporting/informational: in-training eval/walk survived_frac=1.0 for the whole run (no fall-based gait break) and reward quarters [791.8,2711.7,2938.8,2972.0] rising then plateauing in the last two quarters -- exactly the RL-erosion signature (reward saturates once the cheap near-zero-turn optimum is reached, not a still-training case the 08-21 ruling would ask to extend). Why: this was the direct test the turnpay-acq1 FAIL named -- 'can paying 5x more for turning defend authority already present.' Answer: no. PPO keeps finding a cheaper near-zero-turn policy regardless of the yaw term's relative weight; reward-magnitude retention is now refuted at both 1x and 5x. Caveat: the standard gate/owncfg (stochastic) and my own purewalk_det (det progress_ratio/gait_valid) reads were still mid-flight on-pod at verdict time (CPU-contended pod running 4 eval jobs at once, ~35% through the video set after 50min) -- left running, informational only, does not change this verdict since the PASS bar was already missed on the decisive clause alone; next cycle should pull them to close the campaign record but no GAIT-BREAK signal is expected (rise/lower use a separate GRU core from walk's yaw term, and in-training walk survived_frac stayed 1.0 throughout). Next: reward-magnitude retention (1x, 5x) is closed for this campaign; the gate's own named escalation is a per-tick reward-vs-value credit-assignment trace (why does the value function not price defending an existing behavior even when the immediate reward is 5x higher) -- that is new tool-building work, not something to launch blind this cycle; left for the next standwalk cycle/dig-in to design once -s1's own joint read closes.

