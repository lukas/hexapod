# cw-standwalk-stage2-dualbc6-turncap-mirroraug-yawcredit-gradclip0p15-canary-s1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: CANARY_FAIL

**created**: 2026-09-01T12:06:18+00:00

**pod**: hexapod-mjx-train-5

**steps**: 2000000

**parent**: cw-standwalk-stage2-dualbc6-turncap-mirroraug-yawcredit-gradclip0p15-canary

**wandb_id**: mtf7nzbp

**hypothesis**: Plain English: gradclip0p15 (seed 0) gave the best turn-authority result of the whole campaign (wz_med 0.198/-0.200, overshooting the 0.083/-0.138 control, AND fixing the walk-quality collapse) -- is that a basin-luck fluke of this one seed, or does the tight trust-region clip reproduce on a second seed the way the mirror-augment BC-base fix did (turnpay-canary/-s1, 2/2)? Cheap 2M seed-robustness check BEFORE the 38M acquisition continuation (already launched off seed0) commits budget to a single basin.

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. PASS if seed1's own probe_turn_authority wz_med clears >=0.15 both signs (same bar the seed0 arm cleared with margin) AND own purewalk det gait_valid>=5/6 zero falls AND progress_ratio/slip match control band (prog>=0.35, slip<=3.0). PARTIAL if wz_med lands 0.05-0.15 (real but weaker retention) or gait/progress are softer but not collapsed. FAIL if wz_med<0.05 both signs (matching rr1/gradclip2p0's collapse) or progress/slip collapse the same way -- seed0's result would then be basin-specific, not a recipe property; the 38M acquisition arm still stands on its own seed0 evidence regardless.

**verdict**: CANARY FAIL - MECHANISM (own scope; NOT a refutation of the grad-clip mechanism itself -- see caveat). Ran the gate's own two decisive instruments myself on-pod (train-5, idle): probe_turn_authority (own 96-key TURNCAP_CFG_SET, wz_cmd=+-0.25, seeds 0/1) and a no-video purewalk det harness (--modes walk --per-mode 8 --start-jitter-panel). Clause 1 (turn authority >=0.15 both signs) CLEARS with a wide margin, even beating seed0: wz_med pos 0.217/0.218 (avg 0.218) and neg -0.216/-0.200 (avg -0.208) vs the >=0.15 bar and seed0's own 0.198/-0.200; fell=false on all 4 probe reads. Clause 2 (own purewalk det gait_valid>=5/6 zero falls) MISSES: gait_valid itself reads 8/8 both submodes (no sacrificed legs), but 4/8 walk/det and 3/8 walk_startjitter/det episodes actually TERMINATED on an over_current safety trip (7/16 = 44% fall rate) -- seed0's own read was gait_valid 8/8 with ZERO terminations, ZERO falls; this seed does not reproduce that. Clause 3 (prog>=0.35/slip<=3.0 vs control) is mixed: progress_ratio 0.39/0.38 clears fine (close to seed0's 0.384/0.402), but slip/m 6.57/5.71 is ~2x the 3.0 cap, landing in the SAME range as the two already-closed collapse siblings (rr1 slip 5.34/5.76, gradclip2p0 slip 5.63/5.67) even though this seed's progress did NOT collapse the way theirs did. Since the PASS bar requires all three clauses and 2 of 3 miss, this canary FAILS by the gate's own letter -- per the gate's own text, 'seed0's result would then be basin-specific, not a recipe property' for the WALK-QUALITY/STABILITY half of the result specifically. IMPORTANT CAVEAT the binary verdict vocabulary can't carry: the actual NEW mechanism under test (train.yaw_credit_grad_clip=0.15, the tight trust-region clip on the yaw-decomposed critic's extra actor step) reproduced and even exceeded seed0 on the ONE clause it was invented to fix (turn authority) -- it is the walk-quality/stability of the underlying dualbc6/mirroraug base checkpoint at THIS seed, not the grad-clip addition, that shows a basin-specific stability gap (over-current drag/falls). Does not retroactively touch the already-verdicted seed0 CANARY PASS or the already-launched 38M gradclip0p15-acq1 (per the gate's own text, that arm 'stands on its own seed0 evidence regardless' -- left untouched, still training). Evidence (pulled to controller): logs/ckpt_eval/turn_probe_yawcredit_gradclip0p15_canary_s1.json, logs/ckpt_eval/purewalk_gradclip0p15_canary_s1_det.json/report.json. Standard stochastic multi-mode gate/owncfg/mixedsession (video-every=1) left running on-pod (train-5) for later supplementary video -- informational only, cannot flip this read (falls already directly observed). Next: do not spend acquisition budget on a fresh seed1 lineage off this basin without first checking whether more RL steps (i.e. reading gradclip0p15-acq1 once it finishes) narrows or widens this stability gap on seed0's own longer run; treat gradclip0p15 as the reference recipe for TURN AUTHORITY specifically, not yet as a proven zero-fall walk-quality fix across seeds.

