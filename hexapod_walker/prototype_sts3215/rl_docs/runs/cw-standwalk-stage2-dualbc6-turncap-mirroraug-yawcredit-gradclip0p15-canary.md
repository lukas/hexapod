# cw-standwalk-stage2-dualbc6-turncap-mirroraug-yawcredit-gradclip0p15-canary

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: CANARY PASS

**created**: 2026-09-01T11:03:07+00:00

**pod**: hexapod-mjx-train-3

**steps**: 2000000

**parent**: cw-standwalk-stage2-dualbc6-turncap-mirroraug-yawcredit-canary-rr1

**wandb_id**: u64d1bho

**hypothesis**: Plain English: dose-bracket sibling of this cycle's gradclip0p5-canary (launched moments earlier, batched per operator 08-22 batch-the-grid rule instead of dribbling one clip value per cycle) -- does a TIGHTER trust-region clip (0.15, ~3x tighter than 0.5) on the same coef=1.0/vf_coef=0.5 yaw-advantage actor step recover parity with the matched coef=0 control (pos avg 0.083/neg avg -0.138) more fully than 0.5 does, confirming a monotonic dose-response (tighter=better, i.e. update SIZE is the whole story) vs the alternative that even the tightest clip still regresses (update DIRECTION is the problem, not size, ruling out grad-clip as a class of fix entirely)? Same parent/init/seed/recipe as gradclip0p5-canary and the rr1 FAIL, single lever changed (clip value only).

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. Read jointly against the control (cw-standwalk-stage2-dualbc6-turncap-mirroraug-yawcredit-ctrl-canary, wz_med pos avg 0.083/neg avg -0.138), the grad_clip=0 FAIL sibling (rr1, pos avg 0.028/neg avg -0.097), and the grad_clip=0.5 sibling (read jointly when both finish). PASS/PROMOTE if final (2M) probe_turn_authority wz_med clears the control's own final within 0.01 on BOTH signs AND det walk gait_valid>=5/6 zero falls AND purewalk det progress_ratio within 0.03 of the control's own. INFORMATIVE if it beats the grad_clip=0 sibling by >=0.02 both signs but still short of full parity -- compare against the 0.5 sibling to place the dose-response curve. FAIL if within noise of (or worse than) the grad_clip=0 sibling at this tighter dose -- strengthens the direction-not-size read.

**verdict**: CANARY PASS - MECHANISM FIX CONFIRMED (best turn-authority result in the whole yaw-credit/turncap campaign): the trust-region clip on the yaw-decomposed critic's extra actor step (train.yaw_credit_grad_clip=0.15) does not just recover parity with the matched coef=0 control -- it OVERSHOOTS it on both signs and repairs the walk-quality collapse the unclipped dose caused. All 3 pre-registered clauses clear: (1) own probe_turn_authority (TURNCAP_CFG_SET, wz_cmd=+-0.25, seeds 0/1, run myself on-pod train-3): wz_med pos +0.201/+0.195 (avg 0.198) vs control's own 0.083 (delta +0.115, ~2.4x); neg -0.200/-0.201 (avg -0.200) vs control's -0.138 (delta -0.062, more negative/better) -- both FAR outside the 0.01-both-signs PASS band, approaching the scripted-gait ceiling (~0.21) and the raw pre-RL mirroraug distillation seed (0.148/-0.152). fell=false all 4 probe reads. (2) det walk gait_valid: built the harness's own purewalk det read myself (--modes walk --per-mode 8 --start-jitter-panel, run's own training cfg-set minus mode_seq, no-video, own pod train-1) since the standard multi-mode video gate was still mid-flight (video-every=1 class, 1.5-2h) -- gait_valid 8/8 walk + 8/8 walk_startjitter, ZERO terminations, ZERO falls. (3) purewalk progress_ratio: 0.384/0.402 vs control's own (built the SAME way, same cycle) 0.376/0.380 -- delta 0.008/0.022, both well inside the 0.03 band. BONUS not in the gate text: slip/m actually IMPROVED (2.59/2.22 vs control's 2.96/2.72), both inside the joystick <=2.9 band. Contrast with the two siblings that both collapsed identically: unclipped rr1 (already-closed FAIL) reads prog 0.177/0.159, slip 5.34/5.76 (2x over band); the LOOSE dose gradclip2p0 (this cycle's own other arm, verdicted separately) reads prog 0.176/0.162, slip 5.63/5.67 -- essentially IDENTICAL collapse to the unclipped dose despite its own wz_med showing a small partial uptick. Evidence: logs/ckpt_eval/turn_probe_yawcredit_gradclip0p15_canary.json, purewalk_gradclip0p15_canary.json/purewalk_ctrl.json/purewalk_rr1.json/purewalk_gradclip2p0_canary.json (all 4 built this cycle, own pods train-1/2), frame strips logs/ckpt_eval/cw_standwalk_stage2_dualbc6_turncap_mirroraug_yawcredit_gradclip0p15_canary_gate/walk_det_{0,1}.png (clean six-leg gait, upright, no drag). Root cause confirmed: the extra actor pg step WAS oversized/unconstrained (rr1's regression), and a TIGHT clip (0.15) is not just a safety net but actively BETTER than no mechanism at all -- likely because it lets the yaw-advantage signal through while capping the update norm enough that PPO's own clipped step stays dominant. Sharp dose cliff, not smooth: 0.15 (this arm) fully recovers+overshoots, 2.0 (looser) fully collapses just like unclipped -- the concurrent cycle's own 0.5 arm sits between and will place where the cliff actually is. Next: promoting to an acquisition-scale (38M, matching the turnpay-acq1 precedent) continuation off THIS checkpoint this cycle, plus a seed-1 canary twin to confirm this isn't basin luck before spending the acquisition budget on one seed alone.

