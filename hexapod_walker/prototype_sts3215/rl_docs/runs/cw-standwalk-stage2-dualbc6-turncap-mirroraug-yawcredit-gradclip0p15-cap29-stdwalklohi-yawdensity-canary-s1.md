# cw-standwalk-stage2-dualbc6-turncap-mirroraug-yawcredit-gradclip0p15-cap29-stdwalklohi-yawdensity-canary-s1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: CANARY_FAIL

**created**: 2026-09-03T12:20:11+00:00

**pod**: hexapod-mjx-train-4

**steps**: 2000000

**parent**: cw-standwalk-stage2-dualbc6-turncap-mirroraug-yawcredit-gradclip0p15-canary

**wandb_id**: rfas35tp

**hypothesis**: Seed replicate of yawdensity-canary (seed0): same single lever (walk_yaw_zero_frac 0.5->0.2), second seed closes the pass-rate question before any acquisition-scale commitment.

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. Same instruments as the diet-family canaries, but the eval-time cfg-set for the walk-only read must OVERRIDE to the harsh diet regardless of this runs own (gentle) training cfg: goal.walk_cmd_mode=stress_mix, goal.walk_cmd_resample_s=4.0, goal.walk_cmd_resample_jitter=0.5 (matching resamplematch-canary FAIL-numbers protocol exactly) alongside probe_turn_authority (wz-cmds +-0.25, seeds 0/1) and eval_checkpoint.py --modes walk --per-mode 16 x {dr0,ownDR} x {det,sto}. PASS if dir_err_med or course_err_1s_med clears >=20% below the resamplematch-canary FAIL numbers (38-41deg/9.6-12.6deg) toward the acq1 baseline band (25-34deg/7.9-14.6deg) with slip staying <=~3.8 and ZERO new own-DR falls (turn authority already known strong from the ancestor, must stay >=0.07); PARTIAL if steering doesnt clearly move but nothing regresses; FAIL if turn authority regresses below 0.07, new own-DR falls appear, or the run behaves worse than the ancestor on any axis (implicating walk_yaw_zero_frac as not the driver either, closing the whole turn-authority-lever family and forcing an escalation to a full reward-mechanism change).

**verdict**: CANARY FAIL - MECHANISM (seed1 confirms seed0's closure of the structural turn-authority branch, strictly worse) + DIG-IN RESOLVED: the rise-phase over_current anomaly is GENUINE checkpoint fragility, NOT a mode_seq-mixing instrument defect. (1) STEERING (the pre-registered question): walk-only read (standwalk_yawdensity_canary_s1_walkcheck) dir_err_med 44.7deg dr0det / 45.5deg owndrdet — WORSE than the 38-41deg FAIL band it had to beat by 20%; course_err_1s_med 18.8-24.8deg vs the 9.6-12.6deg band; sto slip 11.1-14.1 (ceiling ~3.8); new own-DR falls present. Every pre-registered FAIL branch hits; walk_yaw_zero_frac 0.5->0.2 does not move steering on either seed. Turn-in-place authority intact (probe_turn_authority_yawdensity_canary_s1.json med_wz_err 0.055, wz_med ~0.20 on 0.25 cmd, TRACKS, zero probe falls). (2) DIG-IN, rise anomaly root-caused: isolated rise-only eval (no mode_seq mixing, DR-0, n=8 det + 8 sto, logs/ckpt_eval/yawdensity_s1_riseAB_cap25own on train-5) reproduces the falls HARDER than the mixed gate: 6/8 det, 8/8 sto over_current — instrument-defect hypothesis (b) refuted. Counterfactual at cap 2.9 (yawdensity_s1_riseAB_cap29cf): 0/16 trips (mechanically unreachable) but 11/16 episodes STALL 45-62mm below rise target with servos torque-saturated 8-23s — the behavior is broken regardless of cap; the cap only relabels fall vs silent stall. Video: policy freezes mid-crouch ~2/3 of the episode fighting isometrically at the 2.64A current ceiling, then collapses (2.5) or lurches into over-extension (2.9). (3) WHY seed-asymmetric: family-wide Q3 training collapse (ep_rew quarters s1 [24,29,-200,14] last 58.8; s0 [22,53,-188,72] last 177.2; ancestor gradclip0p15-canary [54,44,-212,-22] last 66) — s1's checkpoint is caught at family-TYPICAL weak recovery, s0 is the outlier; the ancestor's own gate already shows the same rise over_current at 1/6 det, 2/6 sto, and s0's own-DR 1/16 rise-segment trip matches. Lineage weakness modulated by recovery depth, not a new failure class and not seed1-only. (4) CONFIG FINDING (binding for family comparisons): despite 'cap29-stdwalklohi' in the name, BOTH yawdensity seeds were respec'd from the pre-cap29 gradclip0p15-canary ancestor and train+eval WITHOUT safety.max_current_a=2.9 and without stdwalklohi speed keys (walk_speed fixed 0.08; cap = SafetyLayer default 2.5). Name is a misnomer. Corollary instrument fact: sim current = min(|torque|*1.2, 3.0) with 2.2N·m axes ceiling 2.64A, so at cap 2.9 the over_current trip is UNREACHABLE on standard axes — the true-cap29 siblings' 'zero over_current falls' are partly trip-blind, and rise stalls there hide as silent height misses. NEXT: the family escalation to a full reward-mechanism redesign is now UNBLOCKED (the steering gate was NOT miscalibrated); the redesign root-cause chain must also fix rise-stall pricing (stall at saturation is currently reward-survivable) and decide current-cap semantics (trip above the model ceiling = disabled trip).

