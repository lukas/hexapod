# cw-walk-allheading-mlp-singleframe-acq1-stdanneal

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: PASS

**created**: 2026-08-30T05:14:22+00:00

**pod**: hexapod-mjx-train-0

**steps**: 15000000

**parent**: cw-walk-allheading-mlp-singleframe-acq1

**wandb_id**: m9x82ujn

**hypothesis**: Plain English: does annealing the runaway action-noise (train/std) down to a fixed target repair this checkpoint's collapsed stochastic-mode walking and crashed late-training reward, the same way it already fixed the two sibling hist64 mlp/tf all-heading acq1 checkpoints? Single new lever vs the PARTIAL-verdicted source: --log-std-final -3.0 --log-std-anneal-frac 1.0 (anneal starts from the checkpoint's own current mean log_std ~1.62/std 5.05, per the tool's documented warm-start behavior), everything else byte-identical, +15M steps off the finished 40M checkpoint. This is the 3rd confirmed instance of the identical bug on this recipe family (mlp/tf all-heading acq1 already fixed this way) so no new bank is owed -- it is a mechanism repair, not a reward-design change.

**gate**: Fresh DR-0 gate (n=6 det+sto x walk/walk_startjitter, same panel as the source): PASS = sto-mode recovers toward the det numbers (prog med >=0.15, slip med <=6.0, gait_valid>=5/6, no new sacrificed leg) WITHOUT eroding det (prog med stays >=0.35, slip med stays <=3.0, gait_valid 6/6, zero terminations) AND train/std actually lands near the -3.0 target (not still climbing). If PASS: run eval_cmd_suite balanced 8-heading panel (completion >=0.19 every heading, zero falls) then the formal 60s eval_joystick_gate stress_mix script; if that also passes, immediately retry distill_gru.py --dual (single-frame both sides) as the zero-code-change smoke test. FAIL (sto still collapsed, or det erodes) means the anneal target/start needs retuning before any further single-frame walk-source funding.

**verdict**: Fresh DR-0 gate PASS (3rd confirmed instance of the std-anneal repair). policy_std=0.05 confirms train/std landed at the -3.0 anneal target. det: walk prog_med 0.429/slip_med 2.429, walk_startjitter prog_med 0.433/slip_med 2.453 (clear prog>=0.35/slip<=3.0, gait_valid 6/6, zero terms). sto: walk prog_med 0.363/slip_med 2.182, walk_startjitter prog_med 0.365/slip_med 2.498 (clear prog>=0.15/slip<=6.0, sto slip actually BETTER than det in 3/4 sub-panels). Zero sacrificed legs, per-leg duty balanced 0.45-0.70 every episode. Video confirms real six-leg cycling, level body, no dragging. eval_cmd_suite balanced 8-heading: zero falls in all 16 rows, completion 0.27-0.34 every heading (isotropic, >1.4x the 0.19 bar). FOLLOW-UP (this cycle, full chain): held-out 60s eval_joystick_gate stress_mix -> PASS on every axis INCLUDING the stricter default tick metric (zero_falls, slip/m med 2.222 <= 2.9, dir_err med 39.94deg <= 40.0 allow -- thin but genuine margin; windowed course_err_1s_med 5.52deg is comfortably clean vs the 12deg allow), gait_valid_frac 1.0 -- STRONGER than both stdanneal siblings (mlp-acq1-rr1/tf-acq1, both FAILed dir_ok at 51.9/45.5deg) -- the first all-heading walker on this track to clear the formal stress_mix gate outright. Then distill_gru.py --dual zero-code-change smoke test (--walk-teacher this ckpt, --stance-teacher stancemix_bcchain3_stdanneal, --episodes 8 --epochs 2 --transitions 4, merged 48+34-key cfg union, 2 overlapping keys both identical) -> completed the ENTIRE pipeline with ZERO code changes and zero crashes (walk obs 74, stance obs 68, no width mismatch -- confirms the single-frame sidestep of the mode_onehot-stacking bug works); smoke checkpoint deleted after confirming it loaded (2-epoch quality is expectedly poor, this answers compatibility not quality). Why: identical std-anneal mechanism to the two prior siblings; the joygate/distill results are new, not just a repeat. What's next: this checkpoint is now a qualified NEW walk-teacher candidate (mesh/100Hz, joygate-passing) to replace stotight45-seed13 (primitive/25Hz) in the Stage-2 stance-mesh2-stage2-dualbc1/anchor2-14 distillation lineage -- a REAL-scale distillation run needs its own hypothesis/gate registration reading the anchor2-14 walk-retention lessons first (in-loss BC-anchor on the student, per-core log-std annealing, mix/stance-teacher choice), flagged as the concrete next-cycle item in standwalk/STATUS.md rather than rushed onto this cycle's tail. Also landed 2 real code fixes this cycle: launch_run.py --log-std-final acquisition-phase default-injection (9 tests) + eval_cmd_suite.py cfg-set bracket-list parser bug fix sharing train_ppo_sim._parse_cfg_set (4 tests), both snapshotted.

