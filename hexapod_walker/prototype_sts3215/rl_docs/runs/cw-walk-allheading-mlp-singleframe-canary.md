# cw-walk-allheading-mlp-singleframe-canary

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: PASS

**created**: 2026-08-30T02:59:14+00:00

**pod**: hexapod-mjx-train-0

**steps**: 2000000

**wandb_id**: 5mf00dot

**hypothesis**: Plain English: can the same all-heading course-tracking recipe that produced a clean, formal-joygate-PASSING walker (cw-walk-allheading-mlp-stressmix-ft1) work with SINGLE-FRAME observation instead of the 64-frame history stack that recipe used? This matters because this cycle found the hist64 obs is what makes that checkpoint UNUSABLE as a Stage-2 BC-distillation walk teacher: distill_gru.py's --dual dual-core pipeline (the only tool that composes a walk teacher with the acq8m mesh stance champion into one policy) appends its 6-wide mode-onehot PER TICK (by design, so it survives obs-history stacking), so under history_frames=64 the composed env's true per-frame width (74+6=80) no longer matches what any hist64 teacher was trained on (74) -- a flat prefix-slice teacher-obs extraction (the tool's core mechanism) cannot cleanly reconstruct either teacher's expected input once frame widths diverge like that (confirmed empirically this cycle via a smoke test: env obs 5120 vs expected 4742, root-caused to this exact mechanism, see standwalk/STATUS.md 08-30 ~03:1x). A single-frame variant of the SAME reward/env recipe would be immediately compatible with the existing, well-tested distill_gru.py --dual pipeline with ZERO new code. Single lever vs the parent: drop --cfg-set obs.history_frames=64 entirely (default 1), everything else byte-identical (same course-income/excess-sway/course-disp reward stack, same walk BC-anchor teacher/coef, same balanced 8-heading diet, same mesh/100Hz env). Prediction-if-true: mechanism-health canary looks the same as the hist64 twins did at 2M (bc_anchor_loss_walk falling, course-income terms live, no NaN/termination explosion) -- promote to a 40M acquisition and re-run the formal 60s joygate; if that ALSO passes, this becomes the Stage-2 walk teacher with no further tooling work needed. Prediction-if-false (worse mechanism health, or a materially worse matched-step reward trajectory vs the hist64 mlp/tf scratch1 canaries' own recorded quarters 73-172): single-frame obs is genuinely load-bearing for this recipe's course-tracking win, and the correct fix is instead extending distill_gru.py's collect() to be history-stacking-aware (reshape-and-reslice per frame) rather than retraining a weaker teacher.

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. CANARY, mechanism health ONLY (no behavior verdict, matching the hist64 scratch1 canaries' own gate convention): PASS if by 2M (1) no NaN/entropy collapse/termination explosion after 500k warmup; (2) train/bc_anchor_loss_walk falling; (3) course-income terms live (reward_walk_course_income nonzero, rising share); (4) reward trajectory not grossly worse than the hist64 mlp/tf scratch1 canaries' own recorded quarters (73-172 band) at matched steps. Healthy -> 40M acquisition with the same eval_cmd_suite balanced-heading gate the hist64 twins used, then the formal 60s eval_joystick_gate stress_mix script (the real bar: zero falls, slip<=2.9, windowed course_err<=12deg) -- if that also passes, immediately re-attempt the distill_gru.py --dual smoke test with this checkpoint as walk-teacher (should now match cleanly, single-frame both sides, zero code changes needed) before funding any acquisition-scale distillation run. FAIL (mechanism sick, or reward materially worse than the hist64 band) closes the cheap-compatibility-fix path and hands the job to fix (a) from this cycle's STATUS entry: making distill_gru.py's teacher-obs extraction reshape/stacking-aware instead.

**verdict**: CANARY PASS (mechanism health, matches the hist64 mlp/tf scratch1 canaries' own precedent signature -- clears all 4 pre-registered criteria). Evidence (W&B 5mf00dot, wandb_history.csv, 2.0M/2.0M steps): (1) no NaN/blowup anywhere (train/loss, value_loss, entropy_loss all finite and stable), train/std rises mildly 0.369->0.396 (healthy exploration, no collapse), approx_kl/clip_fraction settle down over the run; terminations/over_current shows one transient bump (1.33M-1.72M, peak 42) that fully resolves to single digits by 1.8M -- same shared-training-dynamics-wobble shape the precedent canaries showed (peak 86-108, resolved by 1.1M), not a divergent explosion; terminations/truncated stays flat 19-30 throughout. (2) train/bc_anchor_loss_walk: early bump to 0.078 (~490k, anchor warmup) then falls steadily to a 0.006 plateau by 2M -- same falling-after-warmup shape as the precedent pair (tf 0.0041->0.00019, mlp 0.0030->0.00047). (3) course-income terms live: reward_walk_course_income/walk_course_income_support nonzero almost every logged tick (29/31, only 2 exact-zero ticks near 1.77-1.82M), dips through a mid-run trough (supports 0.47->0.21->0.0) then ticks back UP in the final ~250k steps (0.0->0.0003->0.033->0.351) -- textbook match to the precedent's own recorded 'dips through the mid-run valley then ticks back up in the final ~100k steps' signature (tf support 0.22->0.35, mlp 0.30->0.33). (4) reward quarters [69.8, 151.9, 166.0, 113.2] sit inside/near the hist64 twins' own 73-172 band (Q1 slightly under at 69.8, Q4 dips to 113.2 vs the twins' 162-172, but net trajectory still far above Q1, no divergence-down signature). Auto-generated training-video caption at the final checkpoint reads 'walk:ok raise:ok hold:ok' (no formal DR-0/joygate report needed at canary phase per this gate's own text, non-blocking, same convention as the precedent pair). Net: single-frame obs (history_frames=1) reproduces the SAME mechanism-health profile as the hist64/transformer scratch1 canaries that went on to a clean 40M acquisition and a formal-joygate PASS. Per the pre-registered gate, promoting to a 40M acquisition continuation now (cw-walk-allheading-mlp-singleframe-acq1, respec --init-from-source, same eval_cmd_suite balanced-heading first gate the hist64 twins used) -- if that clears the formal 60s joygate too, immediately re-attempt distill_gru.py --dual with this checkpoint as walk-teacher (should now match cleanly, single-frame both sides, zero new code) before funding any acquisition-scale Stage-2 distillation.

