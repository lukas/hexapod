# cw-walkcurr-sac-sv-s1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: PARTIAL

**created**: 2026-08-29T17:37:33+00:00

**pod**: hexapod-mjx-train-5

**steps**: 2000000

**wandb_id**: bqqjyt33

**hypothesis**: Plain English: can off-policy maximum-entropy SAC discover walking where 16 independently-designed on-policy PPO recipe classes all froze in a static stance? The 08-29 decleg-sv wave dig-in showed PPO converges to a stationary micro-quiver that harvests the freeprog income (~+0.4/tick while standing still) and drifts into servo over_current trips, with reward pinned at 167 - an exploration/optimizer failure mode SAC is specifically built against (Haarnoja 2018 walked the Minitaur from scratch; operator-named fallback ladder item (a), pre-registered in walkcurr STATUS Next). This arm: stock SB3 SAC (new --algo sac, snapshot 7964fe2e, smoke-verified end-to-end on train-4 incl. harness auto-load) on the IDENTICAL WALKCURR_SV diet/plant as the failed wave (mesh/100Hz, loaded servos, freeprog income + term_penalty 24, all else zeroed) - the only lever is the algorithm. n-envs 256 / batch 512 / grad-steps 32 / auto temperature. Prediction-if-true: entropy-regularized exploration visits progress-bearing states PPO never samples; env/walk_speed leaves its 0.02 m/s floor toward the 0.05-0.06 cmd band within 2M with ep_len NOT collapsing. Prediction-if-false: same static basin (walk_speed pinned, over_current climb) in both seeds - SAC-on-this-diet closed, escalate to fallback (b) Heess-style terrain diversity. Strongest alternative: SAC quivers HARDER (max-ent bonus rewards action noise) and trips over_current even faster - that outcome also closes the arm but indicts the diet/plant pairing, not exploration.

**gate**: Discovery gate at 2M (repaired litmus per the 08-29 sv-wave dig-in): PASS requires env/walk_speed rising off the ~0.02 m/s floor toward the commanded 0.05-0.06 band AND rollout/ep_len_mean stable-or-rising while it does. A freeprog escape or zero-crossing co-occurring with a terminations/over_current surge and falling ep_len is the KNOWN ARTIFACT and does not count. End eval: rung-1 C-env det fixed-forward panel (n>=6) read for real stepping on video; walk_speed off the floor + rising reward but gate unmet = continue per 08-21; walk_speed pinned at 2M = FAIL for this seed.

**verdict**: First-ever discovery escape in the walkcurr from-scratch program: SAC seed 1 breaks out of the static-quiver/over_current basin that killed all 16 prior PPO mechanism classes AND the sibling decleg-sv/central-sv/phase-sv wave. Evidence: env/walk_speed leaves the ~0.0 floor and climbs into the 0.05-0.08 m/s band (matching the 0.05-0.06 cmd) by 400k steps and holds there the rest of the 2M run; rollout/ep_len_mean does NOT collapse (115->452->~190, noisy but no crash-to-near-zero); terminations/over_current stays at background levels (1-2/window) the whole run -- none of the known freeprog-escape-is-an-artifact signature (over_current surge + falling ep_len) that invalidated every decleg-sv/central-sv arm. Fixed a real infra bug to get this read: eval_checkpoint.py crashed on every SAC checkpoint (unconditional model.policy.log_std access -- PPO-only attribute; SACPolicy has none) so this was the FIRST successful SAC gate eval end to end; fix is policy_action_std() (None for algos without a static log_std), 2 new regression tests, snapshot 2e7bde91, synced to train-5 before re-running. Full rung-1 DR-0 gate: FAIL -- all 24/24 episodes (det/sto x walk/walk_startjitter) terminate via tilt_pitch or tilt_roll, forward_dist 0.015-0.052m, roll_peak only 5-11deg (a real stumble, not a big pitch-over). But gait_valid=true on every episode with real duty-cycle spread (0.14-0.87) and swing_count 1-7 per leg across ALL SIX legs on video (walk_det_0_sheet.png) -- genuine attempted stepping at roughly commanded speed, not the frozen splayed-crouch every other arm showed. Reading it plainly: SAC found a real gait attempt but has not yet learned to stay upright long enough to bank net displacement -- a stumble-and-fall problem, categorically different from (and further along than) the stuck-in-place problem every PPO arm had. Per Next item 0 / the 08-21 ruling this is the PASS branch on the discovery litmus with the full gate unmet: continue, not FAIL. Next: budget-raise continuation off this exact checkpoint (same SV diet, same algorithm, longer horizon so falling-avoidance can actually be reinforced through term_penalty).

