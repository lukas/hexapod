# cw-walkscratch-easy0905-headset-base-s0c1-noiseonly-c1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: fail

**created**: 2026-09-05T19:32:05+00:00

**pod**: hexapod-mjx-train-4

**steps**: 2000000

**parent**: cw-walkscratch-easy0905-headset-base-s0c1-dgfresh

**wandb_id**: xyz4gzvh

**hypothesis**: Isolation control for headset-base-s0c1-dgnoise-c1 (this cycle, still training): that arm changes TWO things vs the plain undosed s0c1 2M canary at once if read naively (walk_duty_gate ON + a much higher --log-std-final). This arm isolates the noise variable alone: identical seed=0/--init-from base_s0_c1.zip as every sibling in this family, --log-std-final raised -2.0->-1.2 (same as dgnoise-c1) but NO walk_duty_gate (reward.walk_duty_gate left at its default 0.0, matching the plain undosed s0c1 canary otherwise). If leg-4 duty in walk_startjitter/det climbs on ITS OWN (no pricing needed), raising exploration noise alone is the fix and duty_gate was never necessary; if leg-4 stays parked exactly like the undosed twin, duty_gate (or some other explicit price) is a necessary ingredient and dgnoise-c1's result (if it improves) is a genuine interaction effect, not a noise-alone artifact.

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition at this checkpoint. Compare walk_startjitter/det leg-4 duty directly against the undosed s0c1 twin's own report (logs/ckpt_eval/cw_walkscratch_easy0905_headset_base_s0c1_gate/report.json: duty 0.02-0.05, gait_valid 0/6) and against dgnoise-c1's own report once it lands. Read as: leg-4 duty climbs here (noise alone helps) vs stays flat (duty_gate pricing is the necessary ingredient, not just noise) vs both climb similarly (noise is the whole story, duty_gate was a red herring). walk/det+sto must stay >=10/12 valid with no new falls for any reading to be informative (a collapse here would just mean the noise dose is too hot, not a mechanism finding).

**verdict**: CANARY FAIL - MECHANISM (closes the isolating-control cell of the walk_duty_gate x noise 2x2 grid: 'does exploration noise alone move leg-4 duty, with NO duty_gate pricing at all'). This canary is the plain-noise-only twin of the undosed s0c1 baseline: identical seed=0/--init-from base_s0_c1.zip, --log-std-final raised -2.0->-1.2 (same dose as dgnoise-c1) but reward.walk_duty_gate left at 0.0 (no pricing whatsoever). Evidence: on the pre-registered gated mode walk_startjitter/det, leg-4 duty [0.06,0.05,0.04,0.02,0.05,0.02] (med ~0.045) is STATISTICALLY IDENTICAL to the undosed s0c1 twin [0.04,0.05,0.05,0.02,0.05,0.02] (med ~0.045) and to dgnoise-c1 (duty_gate+noise) [0.06,0.05,0.05,0.02,0.06,0.02] (med ~0.05) -- gait_valid 0/6 all three, leg [4] sacrificed every episode, frame strip shows the same planted/dragging leg-4 pose. policy_std reads back 0.254, identical to dgnoise-c1's own readback, confirming the noise dose landed identically in both arms -- the flat duty is not an underdose artifact. No regression elsewhere: walk/det 6/6 gait_valid, walk/sto 6/6, walk_startjitter/sto 6/6, 0/24 falls across all four modes. This COMPLETES the full 2x2 {duty_gate on/off} x {noise low/high} grid: s0c1 (off/low) med~0.045, dgfresh (on/low) med~0.06, dgnoise-c1 (on/high) med~0.05, noiseonly-c1 (off/high) med~0.045 -- noise alone reproduces the UNDOSED baseline exactly (no effect whatsoever on its own), and duty_gate's own small on-its-own bump (dgfresh's ~0.06 vs baseline's ~0.045) does not survive combination with noise (dgnoise-c1 falls back to ~0.05). Root cause (matches dgnoise-c1's own chain): extra exploration noise keeps the training-time reward factor mobile but never touches the eval-time DETERMINISTIC policy mean, which is what actually walks the gate. FINAL SYNTHESIS: every cheap companion lever to walk_duty_gate (bake-in-early via dgfresh, revive-noise via dgnoise-c1, noise-alone via this run) is now closed on the base/non-gSDE family, matching the already-closed gSDE family -- walk_duty_gate and plain exploration-noise scheduling are BOTH fully refuted as fixes for the leg-4/marginal-leg-favoritism pathology on this rung, across every combination tried. No further duty_gate-class or noise-schedule-class arm should be funded anywhere in the headset-base family. Next: the marginal-leg-favoritism question needs a genuinely new per-leg-utilization mechanism (a hard minimum-duty/minimum-swing-count price that cannot be satisfied by a rare token swing or by noise around the mean, not a training-time completion score) with its own design+bank pass before further spend -- no cheap variant remains untried.

