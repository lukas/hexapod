# Scale-out GPU train pods (2026-08-09, operator: ONE POOL, >=4 slots/machine).
# hexapod-mjx-train-4..15, pinned 4 per node (cpu request 24 so 4 fit
# beside the controller's 24 cores on g142d86; train-0..3 on g131eec
# keep their original 26-core spec).
#   g142d86: train-4..7 (+ controller)   g129004: train-8..11
#   g13e3e6: train-12..15                g131eec: train-0..3
#
# 2026-08-09: g12ba48 was CORDONED by CoreWeave and train-12..15 were
# deleted (Pending 3h+, no node fits 4 more 24-core pods) and removed
# from guardrails gpu_pods. RESTORED 2026-09-10 (idle-kick cycle):
# node g13e3e6 (8x H200, fresh, 8h old) appeared as the 4th live node
# with zero train pods — repointed 12..15's nodeSelector here from the
# dead g12ba48 to g13e3e6 per this file's own standing instruction,
# applied, re-added to guardrails gpu_pods. Expect them Pending until
# the node's `cw-hpc-verification` health-check pod (holds all 8 GPUs
# for diagnostics on a freshly provisioned node) releases — normal,
# not an outage; bootstrap once Running.
# Fleet: 16 slots. Live truth: uv run python rl_move/orchestrator/capacity.py
# Pods may sit Pending while CoreWeave hpc-verification health checks
# hold a node's GPUs; they schedule as soon as the check releases.
# Bootstrap after apply: bash rl_move/orchestrator/bootstrap_train_pod.sh <pod>
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-4
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g142d86
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-5
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g142d86
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-6
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g142d86
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-7
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g142d86
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-8
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g129004
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-9
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g129004
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-10
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g129004
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-11
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g129004
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-12
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g13e3e6
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-13
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g13e3e6
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-14
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g13e3e6
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi

---
apiVersion: v1
kind: Pod
metadata:
  name: hexapod-mjx-train-15
  namespace: default
  labels:
    app: hexapod-mjx-train
spec:
  restartPolicy: Never
  nodeSelector:
    kubernetes.io/hostname: g13e3e6
  containers:
    - name: train
      image: python:3.11-slim
      command: ["sleep", "infinity"]
      workingDir: /workspace
      volumeMounts:
        # 64M default /dev/shm SIGBUS-kills sharded-trainer workers
        # (~58M/normal run; hist16+ layouts exceed 64M) — see
        # coreweave_pod_mjx_train.yaml note, 08-10.
        - name: dshm
          mountPath: /dev/shm
      env:
        - name: MUJOCO_GL
          value: osmesa
        - name: XLA_PYTHON_CLIENT_MEM_FRACTION
          value: "0.30"
      resources:
        requests:
          cpu: "24"
          memory: 48Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "26"
          memory: 96Gi
          nvidia.com/gpu: "1"
  volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 4Gi
